# BlockPy Studio — Development Plan

**Derived from:** [README.md](README.md) (spec draft)
**Last updated:** 2026-07-14
**Status:** Active — Phases 0–5 complete; Phase 6 in flight; Phase 7 (punch list 2) added from the maintainer review of 2026-07-14

This plan turns the specification into an ordered set of engineering phases, milestones, and deliverables. Section references (§) point into the spec.

---

## 0. Guiding principles

1. **Legacy behavior is the requirement.** Where the spec says "verify against legacy," verification is a _blocking_ task that happens before the dependent implementation freezes (§7.1, §9.5, §11.2, §14.4). Phase 0 exists to burn down that list.
2. **Build bottom-up along the package graph** (§5.1): `engine` and `vfs` have no UI dependencies and unblock everything else; `api` fixtures unblock all persistence work; UI packages come last.
3. **Conformance suites are written alongside, not after.** Every milestone's exit criteria include its slice of §16.
4. **Ship behind flags, never big-bang** (§17). Each phase ends in a deployable state against an unmodified blockpy-server.

---

## 1. Phase 0 — Foundations & Legacy Freeze (no product code)

Goal: eliminate specification ambiguity and stand up the monorepo. Everything here is a prerequisite for freezing interfaces.

### 0.1 Repository & tooling

- [x] pnpm monorepo scaffold: `packages/{engine,vfs,editor,blocks,reader,quizzer,textbook,navigation,api,lti-embed,legacy-shim,app}` (§4).
- [x] Shared TypeScript ≥5 `strict` config, ESLint/Prettier, Vitest, Playwright, CI pipeline (lint → unit → e2e smoke).
- [x] Vite build targets: full app bundle, `blockpy-studio.iife.js` embeddable, per-package ESM (§4).
- [x] Dev harness: static page that mounts the app from a hand-written `BootConfig` (no server needed).

### 0.2 Legacy verification appendices (the §17 Phase-0 "freeze" deliverables)

Each produces a checked-in fixture/appendix that becomes a conformance-test input:

- [x] **A1 — Verified filename-prefix table** from legacy `src/files.js` + editor source, including magic names (`!sample_submissions.blockpy`, `!tags.blockpy`, `?toolbox.blockpy`, `?mock_urls.blockpy`, image/summary files) and the `&` vs `?` shadowing rule (§7.1, §7.2). → [docs/appendices/A1-filename-prefixes.md](docs/appendices/A1-filename-prefixes.md)
- [x] **A2 — Event vocabulary** (`events.ts`): complete event-type × field enum extracted from both legacy codebases, incl. reader video events and quiz interaction events; confirm whether events batch today (§14.4). _Review-gate deliverable — research pipelines depend on it._ → [docs/appendices/A2-event-vocabulary.md](docs/appendices/A2-event-vocabulary.md)
- [x] **A3 — Quiz schema fixtures**: real payload samples for every question type from `frontend/components/quizzes/`; generate frozen TS types from them (§11.3). → [docs/appendices/A3-quiz-schema.md](docs/appendices/A3-quiz-schema.md)
- [x] **A4 — Settings-key inventory**: every key read from `!assignment_settings.blockpy` and every `settings-*` query param (§11.1, §15.2). → [docs/appendices/A4-settings-inventory.md](docs/appendices/A4-settings-inventory.md)
- [x] **A5 — Golden transcripts**: record full HTTP traffic of a scripted legacy session (load group → navigate → edit → run → pass → quiz → reading) against a dev server (§16.2). _First transcript recorded via `tools/record-golden-transcript.mjs` (Playwright HAR, scrubbed): [docs/appendices/A5-golden-transcripts.md](docs/appendices/A5-golden-transcripts.md). Extend (passing grader run, authenticated + instructor sessions, history/uploads) before the Milestone 1.2 freeze._
- [x] **A6 — Instructions/markdown extension list** used by the legacy instructions renderer and reader (§11.1, §11.2). → [docs/appendices/A6-markdown-extensions.md](docs/appendices/A6-markdown-extensions.md)
- [x] **A7 — Behavioral notes**: passcode validation mechanism (local hash vs server, §11.1), end-of-group affordance (§9.5), run-artifact persistence rules (§7.5). → [docs/appendices/A7-behavioral-notes.md](docs/appendices/A7-behavioral-notes.md)

### 0.3 Technical spikes (de-risk before Phase 1)

- [x] **Spike S1 — Pyodide in LMS iframes**: _resolved by maintainer testing (2026-07-10)._ SharedArrayBuffer is confirmed unavailable in Canvas iframes, and iframe embedding is a hard requirement — so **compat mode (no SAB) is the engine's primary mode**, with SAB isolated mode as opportunistic enhancement. Load time is an accepted cost of Pyodide (managed via caching/lazy-load), not a go/no-go criterion. Residual work (tuning compat-mode interrupt latency, async input shim) folds into Milestone 1.3 step 4.
- [x] **Spike S2 — Lezer CST → Blockly workspace** (maintainer decision 2026-07-10: blocks are generated from CodeMirror's CST, not from CPython `ast` in the worker — overrides spec §8.2's preference order): **GO.** 78/79 of the BlockMirror round-trip corpus parses in agreement with CPython 3.11 (`ast.parse`); the sole gap is valueless `yield` (upstream-fixable). Comments are CST nodes; error nodes are precisely detectable for B3; ~35 ms for a 1,000-line file. Report: [docs/spikes/S2-lezer-cst-blocks.md](docs/spikes/S2-lezer-cst-blocks.md).
- [x] **Spike S3 — Pedal wheel running in Pyodide** with a real `!on_run.py` from curriculum-sneks (§10.1): **GO.** `pedal 3.0.1` + `curriculum-sneks` install and run unmodified on Pyodide 314 (Python 3.14); the real "Convert Pixels" grader from `courses/bakery_course.json` produces correct feedback for both incorrect and correct submissions in 10–20 ms. Key finding: the environment must call `start_trace()` before running student code (coverage tracer). Report: [docs/spikes/S3-pedal-pyodide.md](docs/spikes/S3-pedal-pyodide.md).

### 0.4 Verification findings & decision log (from the 2026-07-10 appendix pass)

The line-by-line verification invalidated several spec-draft assumptions. Summary in
[docs/appendices/README.md](docs/appendices/README.md); consequences for this plan:

**Plan adjustments (applied to the milestones below):**

- **`quiz.preprocess` is a new capability, not a port** — no legacy engine-preprocessing pathway exists (A3). Moved to a flagged §17-style additive extension.
- **Run artifacts are an extension, not parity** — legacy discards all program-written files; the `filewrite` hook is a stub (A1, A7). Per D3's decision note: run-written files surface in the UI **and persist to the backend as submission artifacts** (ledger LD-3x; ships flagged as a §17 additive extension).
- **Reading completion is trivial** — legacy marks the reading correct immediately on load; scroll/video telemetry is logging-only (A7).
- **Two markdown pipelines, no sanitization, no LaTeX in legacy** (A6) — per D4-A, the rewrite **replicates legacy: no sanitization** (spec §4/§11.1 "sanitized" language is superseded). Pipeline unification still needs checking against real course content (marked vs markdown-it differences, `breaks:true`).
- **Do not re-emit server-fabricated events** — `Session.Start` and (nearly all) `File.Edit` rows are fabricated server-side from `load_assignment`/`save_file`; the quizzer logs nothing (A2).
- **`BootConfig.settings` is `Record<string, string>`** — `settings-*` values are raw strings with per-key coercion, never JSON-parsed (A4). Applied to `packages/app/src/boot-config.ts`.

**Replicate-or-fix decisions — ALL DECIDED (maintainer, 2026-07-10)** in **[docs/DECISIONS.md](docs/DECISIONS.md)**: D1-B (seed shuffle), D2-B (fix logging bugs + central event-id registry with deprecation metadata), D3-A (enforce read-only; plus persist run artifacts to the submission), D4-A (replicate: no sanitization), D5-B (round-trip unknown keys), D6-A (replicate), D7-B (preserve, conditional). Fix outcomes seeded into **[docs/approved-differences.md](docs/approved-differences.md)** (LD-1 … LD-7). Original context table:

| #   | Legacy behavior (cited in appendix)                                                                                        | Options                                                                        |
| --- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| D1  | Matching/dropdown option order shuffled with unseeded `Math.random` per render (A3)                                        | Replicate, or seed it (fix) behind the ledger                                  |
| D2  | `X-Editor.Paste` always logs `{characters: 0}`; event-queue `splice` bug wipes queue tail on retry (A2)                    | Replicate bug-for-bug vs fix as `X-` delta                                     |
| D3  | Python/Markdown editors let students edit `&` read-only files; rename/manual-save paths dead (A1)                          | Enforce read-only uniformly (fix) vs replicate                                 |
| D4  | No sanitization of instructions/reading HTML (A6)                                                                          | Sanitize (spec G-goal, may break courses) — needs a course-content audit first |
| D5  | Legacy `saveAssignmentSettings` drops unregistered settings keys incl. server-only `time_limit`/`protected_ip_ranges` (A4) | Round-trip unknown keys (fix per §14.5 spirit) — recommended                   |
| D6  | `settings-*` params unrestricted for students (can flip instructor UI client-side) (A4)                                    | Replicate (server-side security holds) vs gate cosmetically                    |
| D7  | Hidden pool questions' answers dropped on quiz save (A3)                                                                   | Replicate vs preserve                                                          |

**Exit criteria — MET (2026-07-10):** appendices A1–A7 delivered (A5 first transcript recorded; extensions listed); all three spikes resolved (S1 by maintainer testing, S2 GO, S3 GO); D1–D7 decided and ledgered; CI green. **Phase 0 is complete — Phase 1 may begin.**

---

## 2. Phase 1 — Core Runtime: `engine`, `vfs`, `api`, `editor`, `blocks`

Goal: the spec's §17 Phase 1 — a working coding-problem editor behind a per-course flag, mounted via `mountLegacy()` on unchanged server templates.

### Milestone 1.1 — `@blockpy/vfs` (§7)

- [x] Layered FS storing by (space, basename) with the **role-dependent legacy search orders** from A1 §4a (student `? → & → plain → *`; instructor EVERYWHERE and `_instructor/` orders) — `packages/vfs/src/vfs.ts`.
- [x] `parse/format` adapter — the _only_ place prefixes live (§7.3); full eight-space set (`!^?&$*#` + unprefixed) harmonized per A1 §7.8, plus the verified magic-name registry — `packages/vfs/src/legacy-names.ts`.
- [x] Dirty tracking (by legacy name), change events, role-based visibility/mutability matrix (D3-A: `&` uniformly read-only for students, ledger LD-3) — `packages/vfs/src/permissions.ts`.
- [x] Persistence **mapping** (§7.4): per-file plan → `saveFile` individual / `#`-bundle / manual `saveAssignment` / uploads / never (A1 §4d), incl. bundle wire encode/decode and reset-to-start semantics.
- [ ] Persistence **transport**: debounced autosave through `@blockpy/api`, `autoSave`/`readOnly` gating, stale-version banner, uploads-layer endpoints, LD-3x artifact persistence — lands with Milestone 1.2 (needs the API client).
- [x] **Tests:** VFS/legacy-name conformance suite (§16.1.1) — 28 Node-runnable tests over search orders, visibility, deletion guards, bundles, persistence plans.

### Milestone 1.2 — `@blockpy/api` (§14)

- [x] Typed client for the core endpoint set (`loadAssignment`, `saveFile`, `saveAssignment`, `loadHistory`, `updateSubmission`, `updateSubmissionStatus`, `saveImage`, `logEvent`), validated against the golden transcript — `packages/api/src/client.ts`. Uploads endpoints (`listUploadedFiles`/`uploadFile`/`renameFile`/`downloadFile`), `forkAssignment`, `openaiProxy`, `shareUrl` pending the A5 transcript extensions.
- [x] Auth plumbing per A2 §1.1: `Authorization: Bearer` header only (access token never in the body); the eleven-field base context on every call; `version` = submission version (the frontend's assignment-version corruption is NOT reproduced) — `packages/api/src/context.ts`, `transport.ts`.
- [x] Versioned assignment/submission decoder round-tripping unknown fields (§14.5) + `mergeSettings` implementing D5-B/LD-5 — `packages/api/src/decoder.ts`.
- [x] **Central event-id registry with deprecation metadata** (D2): 25 live + 3 dead + 16 server-fabricated identifiers; `clientMayEmit` blocks server-fabricated/dead types and admits `X-` extensions; `X-Editor.Paste`/`X-IP.Change` carry "untrustworthy before Studio" annotations — `packages/api/src/events.ts`. Logger: per-event POST, legacy retry ladder (+2000 ms linear), 200-entry deduped offline queue with LIFO boot flush; LD-2b (single-entry dequeue) and LD-2c (working IP-change detection) fixed.
- [x] **Tests:** transcript replay harness (first cut of the §16.2 G3 gate) asserting field-set + value parity per endpoint against the recorded HAR, plus transport/queue/decoder suites — 18 tests.
- [x] VFS persistence transport: `Autosaver` binds `Vfs` change events → debounced `saveFile` (legacy 1000 ms `TIMER_DELAY`, bundle coalescing, `autoSave`/`readOnly` gating, immediate `answer.py` save on Run, `version_change` → stale-version banner callback) — `packages/vfs/src/autosaver.ts`, structurally typed so `vfs` keeps no dependency on `api`.

### Milestone 1.3 — `@blockpy/engine` (§6)

Build in this order:

1. [x] Protocol frozen (`EngineJob`/`EngineResult`/`TraceStep`/worker messages); `JobRunner` + `WorkerHost` (Node-testable message handling) + `worker.entry.ts` (browser Web Worker shell) + `EngineClient` with live stdout/stderr streaming per job — `packages/engine/src/{protocol,runner,runtime.py,worker-host,worker.entry,client,loopback}.ts`.
2. [x] Job model + queue with priorities and `on_change` debounce/coalesce (§6.3, E5): user jobs FIFO and preempt background; only the newest on_change survives — `packages/engine/src/queue.ts`.
3. [x] Per-job isolation (fresh `__main__` module, `sys.modules` snapshot/restore, FS staging under `/mnt/blockpy` with LD-3x artifact diff-back §7.5) — verified against real Pyodide in the Node suite. "Restart kernel" = `EngineClient.restartKernel()` (worker respawn + fresh interpreter, verified end-to-end).
4. [x] Compat mode PRIMARY (S1: SAB dead in Canvas): `interrupt()` on a running job = worker-termination hard stop with automatic respawn; `detectEngineMode` at boot; mode reported per (re)spawn via `onMode` (the `X-Engine.Mode` log source). _Deferred as isolated-mode enhancement: SAB interrupt buffer._ **Interactive `input()` landed 2026-07-13 (spec §6.5), compat-mode included:** when queued inputs (`inputsPrefill`) run dry and the job carries `interactiveInput`, the worker suspends the run via JSPI (`pyodide.ffi.run_sync` on a JS promise; entered through `PyCallable.callPromising`) and posts the pre-reserved `input-request` message; the console renders the legacy prompt + textbox + Enter line (frozen disabled on submit, value pushed into `queuedInputs` like legacy `execution.input()`), the client pauses the wall-clock watchdog while the user types, and the typed values replay into the Pedal grading sandbox so it sees the same stdin the student saw. No JSPI (older browsers, node) = the legacy scripted-inputs-then-EOFError fallback, unchanged. Verified end-to-end in Chromium under `PYODIDE_E2E=1` (suspend → type → resume → output → frozen line). Hardening (same day): a fatal Pyodide error (JSPI stack exhaustion, unbounded recursion) no longer hangs the UI — the worker host catches the execute throw, resolves the job as an error, AND reloads a fresh interpreter so the NEXT run works instead of faulting on the dead one (the user-reported "Maximum call stack size exceeded" on the next `runPython`); the not-initialized guard also posts a result rather than throwing into the void. Live console answers are NOT folded into `queuedInputs` (that made a re-run replay the previous answer instead of prompting again); the Pedal grading pass still receives them via the adapter.
5. [x] Student-relative traceback mapping (`answer_prefix` lines subtracted, §6.3) and persistent REPL namespace (§6.4). Instructor phases execute; the Pedal environment contract (`set_source → start_trace → run → tifa → exec → resolve`, per Spike S3) is Milestone 1.5.
6. [x] Opt-in `sys.settrace` tracing (E3): compact per-line events with truncated-repr variable snapshots and student-relative lines, 10k-step storage cap — powers the Trace/State Explorer.
7. [x] Time limits (§6.2): client-side wall-clock watchdog (compat hard stop → `TimeoutError` result) + tracer step counter (`limits.traceSteps` → `TraceLimitError`); legacy `execLimit` maps to these.

- **Tests:** engine conformance suite headless in Node (§16.1.3 seed: a handful of curriculum graders from Spike S3).

### Milestone 1.4 — `@blockpy/blocks` + `@blockpy/editor` (§8)

- **Visual parity requirement (maintainer, 2026-07-10; spec B6):** the interface must be basically the same as the original BlockPy interface, **especially layout and color** — region arrangement (instructions pane, view toggles, run controls, console, feedback pane, file tabs) and the palette are conformance targets like any legacy behavior. Icons and fonts MAY change only if relatively similar, sensible, and the change improves usability/accessibility; propose such changes individually with rationale. Reference fixture: [docs/appendices/A8-ui-parity.md](docs/appendices/A8-ui-parity.md) (legacy layout + palette extraction).
- [x] CM6 setup: Python language, syntax lint (from the shared Lezer parse — same B1–B3 gate as block generation), autocomplete, Tab/Shift-Tab indent, Ctrl-Enter run, line-highlight API (`editor-error-line`/`editor-uncovered-line`/`editor-traced-line` classes per A8) — `packages/editor/src/dual/text-editor.ts`. Merge view: history mode shows a read-only CM6 side-by-side diff (`components/HistoryDiffView.tsx`) — see the History note in the chrome bullet.
- [x] Parsing: the Lezer CST from `@codemirror/lang-python` drives both diagnostics and block generation (maintainer decision, see §0.3 S2) — `packages/blocks/src/cst/{parse,to-ast}.ts`. B3 "blockable" gate tolerates exactly the zero-width valueless-`yield` error node, closing the S2 known-gap (all 79 corpus programs parse). Engine-side CPython `ast` remains available to Pedal (§10.1) only.
- [x] Blockly block set + Python generator + CST→workspace builder — full port of BlockMirror's 44 `ast_*.js` modules (block defs + `forBlock` generators + IR→XML converters, quirk-for-quirk), the `text_to_blocks.js` orchestrator (comment/peer layout, chop-and-retry raw-block fallback), generator shims (4-space indent, `___` blank, case-SENSITIVE variable names re-implemented against Blockly 11 `Names` internals), and the legacy signature tables (builtins/methods/turtle/plt). Blockly pinned to 11.2.1 (legacy version). Blocks carry `line_number` attributes for §8.3 block↔line mapping; split-view sync wiring lands with the editor.
- [x] Three view modes with legacy semantics — `DualEditor` (`packages/editor/src/dual/dual-editor.ts`) is the full BlockMirror port: mode tables (block 100%/split 60-40/text 100%), 675px responsive stacking, quiet-set sync loop, `outOfDate_` deferral, read-only overlay, change-listener contract. **B3 note (verified against legacy):** legacy has NO mode lockout — unparseable text degrades to `ast_Raw` blocks via the chop-and-retry loop, which the orchestrator ports exactly; "no blocks from a recovered tree" is enforced inside the converter. React wrapper `DualEditorView`; live in the dev harness (Playwright-verified: blocks render, text→blocks sync, mode toggle). _Chrome toolbar (fa-th-large/fa-columns/fa-align-left toggle buttons per A8) lands with the editor chrome._
- [ ] Instructor-configurable toolbox from legacy settings keys (B4, fixture A4) — presets fully ported (`packages/editor/src/dual/toolboxes.ts`: normal/ct/ct2/minimal/full/empty + `getFunctionBlock`); pending: the `toolbox` settings-key subscription and `custom` = `?toolbox.blockpy` wiring.
- [~] Editor chrome — **first slice landed** (`packages/editor/src/chrome/` + `styles/`): A8 §1 row structure with legacy class hooks (`.blockpy-content` parchment frame, header + instructions, console, feedback, python toolbar, blockmirror mount), instructions pane on `marked` with `breaks:true` + `target=_blank` post-process (A6 §1; D4-A **no sanitization** — test-pinned), Run/Stop button with the A8 §4.1 state classes (incl. wiring the legacy-dead `blockpy-run-error`, A8 §5.2), Reset, view-toggle radios (fa-th-large/fa-columns/fa-align-left), feedback pane with the full Pedal category→`label-*` badge mapping (A8 §4.5), console streaming via the engine-agnostic `RunController` interface, zustand chrome store. CSS: `styles/tokens.css` (A8 §2.2 normative values), `styles/blockpy.css` (blockpy.css + bootstrap_retheme + BlockMirror rules, CM5 selectors twinned for CM6), `styles/bootstrap-subset.css` (pinned BS 4.6 defaults — resolves A8 §5.1). Live in the dev harness; 4 Playwright + 9 chrome component tests. _Engine adapter landed_ (`packages/app/src/engine-adapter.ts`): Run executes `student.run` jobs through `EngineClient` (module-worker via Vite; lazy first-Run boot per R7/§16.3; Stop = compat-mode hard interrupt; errors map to syntax/runtime feedback categories with student-relative lines §6.3). Verified in-browser: gated `PYODIDE_E2E=1` Playwright test does a real Pyodide run (CDN v314.0.2) — prints output, "No errors" feedback, run-state round trip. Icons modernized to Lucide with per-glyph rationale (`chrome/icons.tsx`, B6). _Pedal `on_run` through the worker landed:_ additive protocol extension (`EngineJob.pedal` request → `EngineResult.feedback`), lazy wheel install inside `JobRunner` (fail-soft per §10.1), adapter chains a grading job after each clean student run and maps resolved feedback onto the pane categories. The grading script is the LIVE `!on_run.py` read from the VFS per run (`RunOptions.onRun`; instructor edits apply on the next run — fixed 2026-07-10, previously a static adapter option captured at boot), with the adapter's `onRunScript` option kept as a no-VFS fallback. _VFS→engine file staging landed:_ `Vfs.stageFiles(role)` resolves every reachable basename per the A1 §4a search orders into the prefix-stripped staging map; each run stages the student view into `EngineJob.files` (so `open('sample_data.txt')` reaches `&sample_data.txt` — browser-verified) and the instructor view into the grading job (grader helper imports, A1 §3). Also fixed en route: queued inputs are snapshotted and cleared at run START (legacy run.js:37 `clearInput`), not completion — completion-time clearing raced with inputs queued while a run was in flight and wiped them (caught by the gated e2e). Verified: `PEDAL_IT=1` Node integration (correct→complete, incorrect→gentle, same-interpreter student runs) and `PYODIDE_E2E=1` browser round trip (run → output → **Complete** badge → wrong answer → instructor hint). This is the client half of the §14.3 final-feedback ordering; the `updateSubmission`/`markCorrect` calls attach in M1.6 when the API client joins the app. _File tabs + toolbox wiring landed:_ `chrome/FileTabs.tsx` renders the A8 Row-3 strip from the VFS (legacy instructor tab order with empty starred tabs hidden; `&` files carry `uneditable` and open read-only in forced text mode — D3-A/LD-3, browser-verified); `CodingEditor` takes `vfs`/`role`, edits write back through the VFS (feeding the M1.1 Autosaver when the API client attaches), Run always executes `answer.py`, Reset restores from `^starting_code.py` (§7.4), and `resolveToolboxSetting` maps the A4 `toolbox` key incl. `custom` = `?toolbox.blockpy` JSON with the legacy fall-back-to-`empty`, live-reloading via `remakeToolbox`. _Evaluate/REPL + Trace explorer landed:_ console Evaluate follows the full legacy lifecycle (run success → `beginEval` button pinned inside the printer → inline "Evaluate:" input line → frozen disabled-input echo, re-armed per evaluation; `hide_evaluate` gates it) running `student.eval` jobs against the persistent §6.4 namespace; runs collect the E3 trace (`trace: true`), the feedback pane's View-Trace eye swaps in `chrome/TraceExplorer.tsx` (A8 TRACE_HTML markup: step controls, `n / m`, line indicator, Name/Type/Value table — Type derived from reprs since the compact trace stores reprs only), and stepping drives the `editor-traced-line` highlight. Browser-verified under `PYODIDE_E2E=1` (trace stepping + `a + 41` REPL round trip). _Quick-menu internals + footer + passcode + hljs landed:_ `chrome/QuickMenu.tsx` ports the interface.js:117-193 region (mark-submitted text/click ladder incl. `dirtySubmission` tracking, view-as-instructor checkbox, browser fullscreen with legacy alert-on-error, EDIT_INPUTS dialog → store `queuedInputs`/`clearInputs` → `RunOptions.inputs` → engine `inputsPrefill` (the M1.3.4 compat stdin strategy, browser-verified end-to-end), Toggle Images store flag, share button gated like `canShare`, the dead pink bug icon kept dead, `has_clock` wall clock with the A4 §6 inversion note and the legacy `hours%12` → "0:30pm" quirk); `chrome/Footer.tsx` is FOOTER_HTML (8 `server-status-*` badges driven by new store `serverStatus`, message line, identity line, instructor force-load input); `chrome/Dialog.tsx` ports DIALOG_HTML (modal subset pinned into bootstrap-subset.css); `requestPasscode()` replicates the blocking prompt + store passcode awaiting the M1.6 API context; instructions (400 ms debounce) + feedback code blocks highlight via bundled highlight.js — dead code in legacy (no template loads hljs), made real as **ledger LD-10**. 30 chrome component tests + smoke coverage. _Dev console + system-message routing landed (NEW REQUIREMENT, maintainer 2026-07-10 — a Studio extension with no legacy analog):_ engine/system messages ("Loading Python engine…", grader lifecycle) and instructor-code output (Pedal `on_run` stdout/stderr) never touch the student console — they surface in the footer status area (`onExecution` badge + message line, via the new `RunHandlers.system` channel) and in `chrome/DevConsole.tsx`, a secondary dark terminal-style console available only in instructor view (`display.instructor`, driven by the quick-menu "View as instructor" toggle; wired in the dev harness for debugging). The dev console shares the console slot: a header toggle swaps between the two, each side badging the count of entries the hidden console received (`consoleUnseen`/`devUnseen`). This subsumes the legacy quick-menu "instructor stdout" dialog. _Deferred within quick-menu: owner/readonly spying controls, pool seed input (M2); mark-submitted/share/force-load wiring attaches with the M1.6 API client._ _History mode landed:_ `chrome/History.tsx` ports HISTORY_TOOLBAR_HTML + `BlockPyHistory` (toolbar History button gated on a `loadHistory` callback per legacy `isHistoryAvailable`; Start/Previous/selector/Use/Next/Most-Recent controls; the legacy option list quirk-for-quirk — pretty timestamps, remapped captions, `_instructor.`/Compile/Intervention filtered, hidden-assignment LMS rows dropped, File.Create navigable-but-not-selectable; most-recent edit preselected; failure = legacy error dialog). The editor pane in history mode is `components/HistoryDiffView.tsx`, a read-only CM6 side-by-side merge diff (selected version vs current code) — Studio's upgrade over legacy's plain read-only content swap; "Use" adopts the version through the normal write path and exits. Harness ships a canned log until the M1.6 API client attaches. _"Add New" file menu landed:_ `chrome/AddNewMenu.tsx` ports the FILES_HTML dropdown + `ui.files.add` ladder quirk-for-quirk (items hidden once contents exist; the click-less Tags item kept dead; legacy defaults — `images.blockpy` = `{}`, toolbox = pretty-printed normal-toolbox JSON; On Change/On Eval hidden on existence) and the NEW_INSTRUCTOR_FILE_DIALOG (filename, extension-derived filetype display, `!`/`?`/`&` namespace selector; `^` prefix for starting files, bare for student files); created files open immediately, `addIsVisible` = instructor ‖ !`hide_files` (default TRUE per A4 §5). Fixed en route: starred instructor tabs now hide only when the file is ABSENT — an existing-but-empty `!on_change.py` is the configured post-Add state and stays visible (legacy `onChange !== null` semantics). _Uploads wiring landed 2026-07-11 (with the M1.6 uploads layer):_ `chrome/ImagesManager.tsx` is the legacy "Image" editor port (IMAGE_EDITOR_HTML + images.js) — any `images.blockpy` tab swaps the code editor for the per-placement "Available Files" tables (previews, Delete/Rename gated by `canModify` = instructor ‖ own submission/user placements) and the upload form (placement select instructor-only, filename pre-filled from the chosen file; rename via the legacy plain prompt; delete = upload with empty contents + `delete` flag). The API grew the multipart path: `Transport.postForm` (browser-set boundary, finite legacy `_postBlocking` attempts + FAIL_DELAY backoff) under `listUploadedFiles`/`uploadFile`/`downloadFile`(raw-text)/`renameFile` with the exact wire field names. Remote files reach RUNS like legacy `reorganizeFiles`/`downloadRemoteFiles` (files.js:669-737): the listing registers `filename→url` on the VFS, each new file's body is fetched and cached (`Vfs.setRemoteContents`), and `stageFiles` stages remote bodies at the LOWEST priority (consulted last, A1 §4a); `preload_all_files` triggers the fetch at assignment load. Gated e2e proves `open('capitals.txt')` reads a server upload end-to-end; the smoke suite covers the manager UI. _Still pending here: the `preload_files` JSON variant (with CORGIS, §10.4)._
- [x] Minified editor variant (§8.4) — `chrome/MinifiedEditor.tsx`: text-only by default (blocks via `mode` prop), short height, no tabs/instructions panes, save/submit stripped (A7). Layout (maintainer 2026-07-11): two columns — LEFT output console above a compact feedback area (same category badges/LD-10 highlighting as the main pane), RIGHT the Run/Stop/Reset toolbar above the editor; stacks below the 675px breakpoint. The root carries `.blockpy-content`, so colors match the full editor exactly (parchment frame, warm borders, scoped badge/button rules). All state is per-instance React state (a reading hydrates many at once — the singleton chrome store is not shared); the page engine is shared via a common `RunController`, each run in a detached namespace (§6.4). Reading-id event logging + ephemeral-VFS file staging attach with the M2.3 `reader`. The dev harness has a view-swap button (full ↔ minified, code round-tripping through the VFS); `DualEditorConfiguration.indentSidebar` (default true = legacy `updateGutter`) lets the minified variant drop the text-mode alignment sidebar it has no toolbox for.
- [x] **Tests:** round-trip suite over the BlockMirror corpus (§16.1.2) — **79/79 exact-text fixed points (double trip)**, headless (jsdom + Blockly headless workspace). Corpus fixture extracted by `tools/extract-blockmirror-corpus.mjs`. Two corpus entries conflict with actual legacy behavior (legacy's own runner masked failures via silent `console.assert`+`break`): #42 multi-dim subscripts now round-trip losslessly (ledger LD-8), #73 bare hidden `plt` import keeps legacy suppression (ledger LD-9, pinned as known-delta in the suite). Plus 34 CST→IR unit tests and a pipeline smoke suite; 214 tests green workspace-wide.

### Milestone 1.5 — Integrations needed for parity (§10)

- [x] **Pedal environment implemented** (`packages/engine/src/pedal.ts` + `pedal-env.py`) — **rewritten for legacy-wrapper fidelity 2026-07-12 (maintainer review):** the environment now builds on `pedal.environments.blockpy.setup_environment` exactly like legacy's WRAP_INSTRUCTOR_CODE (on_run.js), which supplies the HtmlFormatter, source `verify`, tifa-unless-skipped, `set_input`, and the load-bearing `start_trace → run` ordering in one call. Ported wrapper behaviors that were previously missing: bakery `student_tests.reset()` per pass; the preloaded instructor namespace (`parse_program` + `pedal.sandbox.commands`/`pedal.core.commands` star-imports — graders rely on these without importing); `skip_run` (disable_instructor_run) and `skip_tifa` (disable_tifa) settings plumbed end-to-end (settings blob → CodingEditor → RunOptions → PedalJobRequest); pool seeding by submission id — **LD-22: legacy seeded BEFORE setup_environment whose report.clear() erased the seed, so legacy pools were never actually seeded; Studio seeds after, making the documented intent real**; the Submission now carries the STUDENT file view (legacy getAllStudentFiles) while the instructor view stays on disk; `final.instructions` (questions → instructions-pane replacement), `final.positives` (green stars, else_message quirk), `final.systems` log/debug (→ dev console), and the first-error line (`DATA['location'].line` → editor-error-line highlight) all extracted and presented. **on_eval landed (on_eval.js port):** console evaluations chain an `instructor.on_eval` job when `!on_eval.py` exists — pedal `evaluate()` re-runs the expression in the RETAINED grading sandbox (feedback cleared, report kept), the on_eval script execs, and the re-resolved feedback presents + grades exactly like on_run's (EvalOutcome grew feedback/grade/instructions; engine gained `PedalEnvironment.evaluateGrade` + `PedalJobRequest.evaluation`). Client presentation parity (feedback.js:182-264): messages run through the legacy markdown pass at presentation (`renderFeedbackMessage`, marked + the `<pre>\n` doubling quirk), and the Instructor/"explain" → "Instructor Feedback" and Instructor/"No errors" → `no errors` remaps applied case-insensitively (Pedal 3 says "No Errors", Pedal 2 said "No errors"). Fail-soft `system_error` unchanged. **Corpus regression improved: 213/213 execute, 195 clean (was 194), 0 env-level crashes** via `tools/run-grader-corpus.mjs`; gated suites now 15 tests (`PEDAL_IT=1`) incl. namespace preload, skip_run, positives, and the evaluate/on_eval round trip. Known-absent legacy pieces (documented, deferred): the `utility` module (`get_model_info` etc. — needs the bundled-wheel work below), `instructionsPool` on_run extraction (pools, M2 deferral), GracefulExit, and the instructor full-feedback table view.
- [ ] Wheels bundled with the deployment (currently PyPI via micropip): `pedal`, `curriculum-sneks`, `bakery` resolve; **`curriculum-ctvt` is not on PyPI** — needs a bundled wheel, as do `bakery_canvas` and the legacy `utility` module (compat appendix table).
- [ ] Final-feedback → `updateSubmission`/`markCorrect` ordering (§14.3) — editor wiring lands with M1.6. **S3 score-derivation open item RESOLVED (2026-07-11):** full contract pinned in the skulpt-compat appendix — client sends clamped monotonic-max score + RAW run success as `correct` (+ block-PNG `image`); ordering is feedback → POST → markCorrect-on-response (fires even on server failure, a legacy quirk); server-side `full_score()` makes `correct` dominate for non-reviewed assignments, so Pedal's `success=true, score=0` needs no fixup.
- [~] matplotlib (§10.2) — **capture + console rendering landed (2026-07-11):** the runtime forces `MPLBACKEND=Agg` (Agg "cannot be shown" warning silenced), snapshots every open figure to base64 PNG after each run — including errored runs — then `plt.close('all')`; `EngineResult.images` → `RunOutcome.images` → console `image` entries rendered inline (`.blockpy-console-image-output`, quick-menu Toggle Images falls back to text — legacy semantics) in both the full console and the minified editor. Enablers landed with it: `loadPackagesFromImports` before student jobs (numpy/matplotlib imports just work, fail-soft to Python's own ModuleNotFoundError when offline) and site-packages module ADOPTION in the per-job restore (installed packages keep their multi-second init out of every run; stdlib/staged modules still purge per §6.2). Gated tests: `MPL_IT=1` Node capture round-trip, `PYODIDE_E2E=1` in-browser plot → inline `<img>`. **Plot-inspection open item RESOLVED (2026-07-11): no shim needed** — `MockPlt` is Pedal-internal (not Skulpt-side): Pedal's sandbox mocks `matplotlib.pyplot` by default during the grader's re-execution, so `assert_plot`/`get_plots` read that call log independently of the student run's real Agg backend; verified against the real pedal wheel (`runner-pedal.test.ts` correct→Complete / wrong-data→`wrong_plt_data`, PEDAL_IT=1). _Pending: `image` log events + `saveImage` (M1.6 API)._
- [~] §10.4 — **`?mock_urls.blockpy` shim landed (2026-07-11):** the runtime installs a per-job `requests` mock in `sys.modules` before student code runs, resolving the legacy `{filename: [urls]}` table against staged files (prefix-stripped fallback) with the exact legacy IOError texts; no real-network path, matching Skulpt (see skulpt-compat appendix "Student-runtime behaviors"). Offline Node tests cover hit/miss/no-table. **Remote-file fetch into the engine mount landed 2026-07-11** (uploads layer: list → download → `Vfs.setRemoteContents` → staged last in the search order; `preload_all_files` supported). _Pending: CORGIS dataset import (`import weather` style via `filesToUrls`) + the `preload_files` JSON variant._
- OpenAI proxy pass-through (§10.5).
- Skulpt-compat appendix started (§6.7) — living document updated as deltas surface.

### Milestone 1.6 — Boot & shim (enough to deploy)

- [x] `BlockPyStudio.mount()` + `mountLegacy()` assembling `BootConfig` from old globals (§5.2) — `mountConfig(rootEl, config, extras)` is the programmatic core returning a `StudioHandle` (queued imperative bridge: loadAssignment/loadAssignmentData/hide/show/requestPasscode — queuing because legacy pages drive the editor in the same tick as construction while React commits async). `mountLegacy` reads `$blockPyUrls`/`$blockPyUserData`/`$blocklyMediaPath`/`accessToken` plus the page URL (`assignment_id`, `assignment_group_id`, `embed`, `read_only` — Jinja `True`/`False` tolerated); the type-index consts (`QUIZZES` …) are top-level `const` in editor.html (global LEXICAL bindings, not window properties) — probed via window-then-indirect-eval with CSP-safe fallback (`legacy-globals.ts`). `settings-*` params stay RAW STRINGS (A4) and apply LAST over BootConfig.settings in every mount path (§15.2).
- [x] **App now loads assignments through `@blockpy/api` (the §14 client attachment, load half):** boot order matches editor.html:341-348 (inline `assignment_data` beats the id fetch); `loadAssignment` drives the legacy `_postBlocking` badge lifecycle (active → ready/failed) on the footer; the decoded pair maps into the VFS via `assignment-loader.ts` (the `loadAssignmentData_`/`loadSubmission` port: answer.py = submission-else-starting-code, model-bound `!`/`^` files, `!on_change.py`/`!on_eval.py` only-when-configured, `extra_*_files` JSON-dict bundles verbatim); the wire context (`assignment_id`, versions, `submission_id`) follows the loaded pair. `!assignment_settings.blockpy` blob feeds `toolbox`/`hide_files`/`hide_evaluate` (under `settings-*` overrides); `loadHistory` rides the real endpoint (History button disabled when unconfigured — legacy `isEndpointConnected`). The dev harness now serves a stub server from Vite middleware (`/api/load_assignment` etc.), so the smoke suite exercises the REAL fetch → decode → VFS → editor pipeline; a canned offline fallback remains for serverless configs. 9 smoke + full gated `PYODIDE_E2E` pass through the fetched-assignment path.
- [x] `legacy-shim` minimum (§15.1–15.2): `window.blockpy.BlockPy` facade consuming the exact editor.html:263-292 option bag (`optionsToBootConfig`; unknown keys → settings as raw strings, the server-side Jinja settings loop lands there), jQuery-Deferred-compatible `loadAssignment` (`.done/.fail/.always` chaining), `loadAssignmentData_`, `hide()`/`show()` (container display toggle, blockpy.js:1263-1270), `requestPasscode()`, `$MAIN_BLOCKPY_EDITOR` pointed at the facade, `callback.success` captured for the §14.3 markCorrect wiring, `installLegacyShim()` publishing the global. Ships as `blockpy-studio-legacy.iife.js` (own Vite lib build; `worker.format: 'es'` fix applied to BOTH iife builds — the app's was latently broken since the engine worker landed). Dependency direction fixed: legacy-shim → app (was an unused reverse scaffold dep). §15.3 globals (`frontend`, `markCorrect`, `URL_MAP`, …) remain M2.5.
- [ ] Deploy behind per-course flag; Skulpt client stays default. (Server-side: swap editor_includes.html to the shim bundle for flagged courses. Note the iife builds emit a separate CSS file that the template must also include.)
- [~] §14 client attachment, run half — **§14.3 grading sequence + autosave landed (2026-07-11):** `app/submission-sync.ts` ports server.js saveFile/`_postLatestRetry`/updateSubmission + on_run.js:162-175 quirk-for-quirk: per-filename trailing debounce (TIMER_DELAY 1000 ms, latest-wins; immediate answer.py save at run START per run.js:13 — new `CodingEditor.onRunStart`/`onFileEdit` hooks, autosave restricted to the legacy createSubscriptions filename set), score = clamp(0,1) → monotonic max seeded from the loaded submission (wire score is a 0-1 float — `as_float_score`), wire `correct` = RAW success of THIS run (display OR-chain kept separately), `hidden_override`/`force_update: false` payload, badge lifecycles (active→ready/failed-with-message, offline on read-only), and **markCorrect fires on the response regardless of `response.success`** (the legacy quirk) gated only by `!hide && correct` — delivered through the shim's `callback.success` (`MountExtras.markCorrect`). Ordering is presentFeedback-FIRST: the editor's new `onGraded` hook fires after `setFeedback`, verified by a hook-order unit test. Enabler: Pedal's `final.hide_correctness` (the legacy HIDE global, on_run.js:73) now rides `PedalFeedback` → `RunOutcome.grade` (SUCCESS/SCORE/HIDE triple). 11 sync unit tests. **Event logging landed (2026-07-11, §14.4/A2):** the chrome fires a `LogEventFn` hook at the legacy call sites — `Compile` at run start (run.js:14), `Run.Program` with the `{inputs, outputs}` JSON on success / `ProgramErrorOutput` category on runtime errors / `Compile.Error` on syntax errors (run.js:44-85), `Intervention` logged WITH the feedback presentation (extended=true; payload `{message, syntaxError, runtimeError, unitTests}` — the `unitTests` tallies are the countTestCases port, computed in the Pedal env over `MAIN_REPORT.feedback + ignored_feedback` since Pedal 3 split the lists legacy iterated as one), eval lifecycle (`X-File.Add`/`Compile`/`X-Evaluate.Program`/`Compile.Error` on the `evaluations` virtual file), `X-View.Change` (message = block|split|text), `X-File.Reset`. The app forwards to `ApiClient.logEvent` (registry-guarded, offline queue) and adds `X-IP.Change` via the transport hook (LD-2c) + the boot-time LIFO queue drain (legacy checkCaches). Registry findings honored: `File.Edit` rows are SERVER-fabricated from save_file (autosave already feeds them — History is fully real), `Session.Start` is server-side (client emission would double-log), `Session.End` was dead legacy. **Quick-menu server actions + version banner landed (2026-07-11):** mark-submitted click ladder wired to `updateSubmissionStatus` (the wire `status` is a STRING — fixed the client's `number` typing against blockpy.py:567-585; local status updates only on `success: true` per postStatusChange, transport failure = the legacy error dialog); share = client-built link (`base + btoa("group_course_group_assignment_user_ISOdate")`, blockpy.js:635-650) opening the START_SHARE dialog in the chrome (copy-to-clipboard → "Copied!", QR section fails soft with legacy's exact "QR code generation failed." — bundling a QR lib is an open call); footer force-load file input → `loadAssignmentData_` and Update-Submission badge click → `force_update: true` re-POST (blockpy.js:1185-1208); fullscreen event stream completed incl. the legacy `.catch().then()` quirk where Success also logs after the Error path (interface.js:50-70). **`version_change` banner is ledger LD-11:** the legacy client NEVER read the flag save_file returns — Studio surfaces the §7.4 dismissible out-of-date banner, resetting per load. **Block-PNG image landed (2026-07-11):** `DualBlockEditor.getPng()` is the `getPngFromBlocks` port (block_editor.js:322-384) on Blockly 11 — clone `workspace.getCanvas()`, strip offsets, inline every `<style id="blockly…">` sheet (media paths pre-resolved, unlike legacy's `<<<PATH>>>` tokens), renderer/theme classes read off the live injection div, SVG-data-URL → canvas → PNG with the legacy fail-soft ladder ('' on empty/error, SVG URL when canvas export is blocked). Every `updateSubmission` POST (grading AND force-update) awaits the capture and always carries the `image` field ('' when none), exactly like server.js:675-680; the gated e2e asserts the real browser POST contains `image=data:image/png;base64,…`. `saveImage` itself is turtle-only in legacy (`save_turtle_output` → `turtle_output` directory, console.js:467-481) — it lands with turtle support. **Feedback rating region + paste event landed (2026-07-11):** the feedback pane grew the legacy response region (feedback.js:46-74) — thumbs up/down (Lucide pair; legacy's rated solid→regular swap becomes a dim, per-glyph rationale in icons.tsx), collapse/expand persisting to the legacy localStorage key (`BLOCKPY_display.showRating`), the animated "Thank you!", and the quirk preserved verbatim: ANY rating opens the PROMPTED share dialog after 1 s (legacy's `suggestShare` param was dead — blockpy.js:801-813); the prompted variant's message rides the same START_SHARE dialog via a store flag. `X-Rating` logs with the presented feedback's category/label. Instructor header controls: score % + "(reset)" → zero display state + POST `hidden_override`+`force_update` both true (blockpy.js:784-788, `SubmissionSync.resetScore`). `X-Editor.Paste` logs from the editor mount with REAL character counts (legacy's shadowed `const characters` always logged 0 — LD-2a: trustworthy from Studio on). _Pending: `#extra_*_files.blockpy` bundle autosave (with uploads)._

**Exit criteria:** flagged course completes edit → run → Pedal feedback → grade passback on an unmodified server; engine suite passes curriculum regression corpus; golden-transcript replay matches for the coding-problem slice; Drafter deferred to 1.x follow-up (§10.3) if timeline requires — it is not on the Phase-1 gate.

---

## 3. Phase 2 — Full Page Ownership: `navigation`, `AssignmentHost`, `reader`, `quizzer`

Goal: the React app owns the whole `editor.html` body for flagged courses (§17 Phase 2).

### Milestone 2.1 — `AssignmentHost` + routing (§5.3)

- [x] Type dispatch in legacy priority order (quiz → reading → textbook → java → kettle → explain → blockpy); unknown ids fall through to the editor; per-type unmount/remount semantics preserved — **landed 2026-07-11** (`app/AssignmentHost.tsx`, the `loadAssignmentWrapper` port, editor.html:304-338): `classifyAssignment` is the pure membership ladder; dispatch resets the SIX non-blockpy id slots every call (only the active type keeps its id → keyed remount), hides-but-never-unmounts the editor for non-blockpy types (`editor.hide()` semantics), and — legacy-exact — leaves the blockpy id UNTOUCHED in the non-blockpy branch, flipping it only in the editor branch's async completion (`whenDone`). Boot routes the initial id through the dispatch (legacy `loadAssignmentWrapper(current_assignment_id)`); the facade's `loadAssignment` stays the RAW editor load (legacy `editor.loadAssignment`). Type bodies are placeholders until M2.2-2.4 mount real components.
- [x] Java tombstone message ("Java assignments are no longer supported in BlockPy.", editor.html:159 verbatim).
- [x] `loadAssignment(id)` exposed and aliased to `altAssignmentChangingFunction` (§15.3) — published on mount, removed on unmount; smoke-verified end-to-end (dispatch a quiz id → editor hidden/quiz slot mounted/URL updated → back to blockpy → editor + blocks return).
- [x] URL contract: `history.replaceState` updates `assignment_id` in place preserving every other param (`assignment_group_id`, `embed`, …), skipped in embeds; deep links honored at boot (BootConfig/`mountLegacy` parse `assignment_id`). Full-page fallback landed with M2.2 (2026-07-11): `GroupNavStore.navigateTo` prefers the injected SPA dispatch, then `window.altAssignmentChangingFunction`, then navigates to the boot data's per-assignment `url` (the legacy URL_MAP value) and shows the verbatim "~~~ The next problem is loading! Please wait" notice (assignment_groups.html:62-71).

### Milestone 2.2 — `@blockpy/navigation` (§9) — **landed 2026-07-11**

- [x] Dual-rendered header/footer from one store; exact legacy layout, button semantics, `✔` prefixes, CSS class hooks (§9.1, §9.6) — `navigation/GroupNav.tsx` + `store.ts` (the `assignment_group_header` macro port): one `GroupNavStore` drives both instances (`useSyncExternalStore`; legacy stayed in sync only via document-wide jQuery selectors); subordinate filtering (`rejectattr`), INDICES clamp quirks (`INDICES[i+1] || LAST_ID`), UI-updates-before-load ordering, First/Back + Next/Last boundary disabling. Icons: fa-step/chevron glyphs → Lucide SkipBack/Chevron*/SkipForward (same modernization as the editor steppers, A8 §3.2). Geometry note: the header carries the legacy layout.html container padding + border-box reboot itself (`styles/navigation.css`) since the app shell has no container-fluid wrapper; missing spacing utilities (`.m-1/.mt-1/.mb-1/.mr-1/.ml-2`) added to the bootstrap subset.
- [x] Completion box + selector expansion with exact localStorage key (`blockpy_assignmentSelectorExpanded`, string `"true"`/`"false"`, storage-denied guard, re-reads storage per click); `size = min(5, N)` list-box mode; secretive `??` masking (§9.1, §9.3).
- [x] `markCorrect` store action + `window.markCorrect` global alias (no-op on group-less pages, editor.html:109-114; never clobbers a legacy-template-owned global in shim mode); quirk-exact port of assignment_groups.html:124-142 — Next turns green on _any_ markCorrect (A7), unknown/subordinate ids still bump the numerator every call, end-of-group = green-but-disabled Next + full count, no congratulations message (A7 §2). Grading success reaches it through SubmissionSync's markCorrect (composed with `MountExtras.markCorrect`).
- [x] Time-spent clock (tier texts incl. singular/99+-cap/zero-pad precisions, 10 s tick, session→loading→activity mode toggle, error fallback, activity = fetched total + still-ticking session) via `ApiClient.estimateGroupDuration()` (new endpoint method; ids ride the base payload — legacy baked them into a `$.get` URL); `window.ACTIVITY_GET_DURATION` exported when connected (§15.3). Countdown owned by the store (§9.4): 5 s checker with `$TIME_CHECKER_ID` singleton hand-off (so legacy islands can supersede it), `parseTimeLimit` port (per-student `"Nmin"`/`"Nx"` overrides), `"{X} elapsed; {Y} left"` coarse `formatAmount`, clock hidden while active, freeze-after-expiry early returns, instructor exemption, "Time is up!" overlay, `timer_expired`/`timer_cleared`/`timer_error` events — now also active for time-limited blockpy assignments (ledger LD-12).
- [x] **Tests:** 57 unit tests (format/store/GroupNav + 2 App integration) + Playwright navigation suite (§16.1.4): dual headers, subordinate filtering, boundary disabling, markCorrect visuals in both instances, expansion persistence across reload, clock tiers + mode toggle against the dev endpoint, select/Back dispatch through AssignmentHost with URL updates.
- ~~Deferred to M2.5 (§15.3): shim-emitted `URL_MAP`/`INDICES`/`FIRST_ID`/`LAST_ID`/`FULL_SELECTOR_DIV`/`loadNavigation()` compatibility globals for unmodified templates.~~ Landed with M2.5 (`publishNavigationGlobals`).

### Milestone 2.3 — `@blockpy/reader` (§11.2) — **landed 2026-07-11**

- [x] Markdown/HTML pipeline — unified on `marked` replicating the markdown-it observables (**ledger LD-13**): breaks off, D4-A **no sanitization**, in-render hljs fences, the `replace-link` rewrite with the verbatim `startsWith("http")` predicate and unencoded `download_file?placement=assignment&directory={id}&filename={link}` URLs (`reader/markdown.ts`). Settings surface ported in full (`reader/settings.ts`, reader.ts:183-257): youtube/video string-or-voice-map with the `blockpy-reader-voice-choice` localStorage memory, header/summary, slides (own `/^https?:\/\//` predicate), popout (editUrl from `$URL_ROOT` when present; `?&embed=true` quirk kept). Scroll telemetry: the lti.fetchWindowSize postMessage round-trip with escalating `count × 30 s` pings (`Resource.View`/`reading`/`read`, self-echo when unframed); HTML5 video events + YT.Player state changes (`watch`) — YT fail-soft, matching legacy's commented-out iframe API; tab `visibility` events. Reader page CSS re-scoped from the `<reader>` element (editor_includes.html:26-63) incl. the option status colors that belong to the nav header.
- [x] Runnable code blocks: python fence + non-empty part id → Run button → §8.4 MinifiedEditor hydrated in place via portals, sharing the page RunController; the highlighted pre hides on launch (plugins.ts:350-368); python-without-attrs keeps the DOM structure but never gets a button; ts/r fences emit the kettle structure but stay inert until the M2.5 island. Save/submit endpoints stripped (the minified editor has no persistence). Events attach to the READING's ids — `ApiClient.logEvent` gained payload `overrides` and the app supplies the reading's assignment/submission ids (assignment_interface.ts:266-284, §12). _Pending: part-id `saveFile` composition (`##### Part` injection, plugins.ts:284-348) so runnable-block edits persist — needs the partId file contract (M2.5/§15)._
- [x] Completion rule per A7 §4: `markRead` fires immediately on load when a submission exists (updateSubmission `{status: 1, correct: true}` with the reading's ids overriding the base payload); server echo drives local correct + the navigation `markCorrect`; rejections surface via the error banner; anonymous loads never mark. Exam gate: `start_timer_button` hides the group selector for unstarted students until "I am ready to start the exam!" posts the new `ApiClient.startAssignment` (blockpy/start_assignment); the started exam feeds the header countdown through `onTimeLimitInfo` → `GroupNavStore.setTimeLimit` (the legacy reader IS the AssignmentInterface running the checker on its own pair). `asPreamble` prop retained.
- [x] Subordinate quiz composition — closed with M2.4 (2026-07-11). The legacy mechanism runs the other way from README §11.2's phrasing: there is no reader-renders-quiz path; the QUIZ renders its reading as a preamble above itself via `settings.readingId` (quiz_ui.ts:194-208), with `markCorrect: ()=>{}` so the preamble never touches navigation. Studio wires exactly that (`Quizzer.renderReading` → `<Reader asPreamble>` with a no-op markCorrect); `asPreamble` prop retained.
- [x] **Tests:** 24 reader unit tests (markdown fences/rewrites/parity options, settings + voice memory, Reader load⇒markRead/anonymous/error/runnable-hydration/exam-gate/read-ping) + the §16.1 smoke test: select the reading in the group nav → content renders, the auto `updateSubmission` POST carries `correct=true&status=1` with the reading's id, the nav flips ✔/count/green-Next from the echo, relative images rewrite through download_file, Run hydrates the minified editor, Back restores the live editor.

### Milestone 2.4 — `@blockpy/quizzer` (§11.3) — **landed 2026-07-11** (preprocessing pending)

- [x] All A3 question types: 10 rendered (true/false, multiple choice [markdown options], multiple answers [raw-HTML options], matching, multiple dropdowns + fill-in-multiple-blanks [`[blank_id]` bodies → slot spans → portaled controlled inputs, `[[literal]]`/`\[escape]` grammar preserved], short answer, numerical, essay, text-only) + `calculated_question`/`file_upload_question` as pass-through rendering the verbatim "I have no idea what this is!" fallback (`quizzer/types.ts` freezes A3 §8; `QuestionView.tsx`). Status squares (unanswered/answered/error/correct/incorrect) as per-question anchors + the quick-jump Overview row; question bodies/choices render through the reader markdown pipeline (same `markdowned` sharing as legacy, A6).
- [x] Pooling: `subsetRandomly` sin-seed PRNG port; membership seed = submission id (SEED) / id+attempt (ATTEMPT) / 0 (NONE; GROUP inert); backfill default ATTEMPT frozen (A3 open q. 5); instructor seed field takes effect on next start (legacy-exact); matching/dropdown option shuffles SEEDED with the pool seed + per-question offset (D1-B, **LD-1**). Attempt lifecycle: READY→ATTEMPTING→COMPLETED derived from `attempt.{attempting,count}`; Start clears feedback, re-pools, saves, does NOT clear answers; `attemptsLeft` verbatim strings incl. the "0 attempts left." quirk; `mulligans` honored (server-granted). Timed quizzes feed the header countdown via the same `onTimeLimitInfo` → nav-store path as readings (§9.4/A3 §3.6).
- [x] Feedback flags: IMMEDIATE per-question boxes (bg-success/danger/dark, score×points rounded) + NONE/SUMMARY student hiding with the verbatim completion-bar texts; instructors viewing not-as-student always see feedback/scores/ids/pool badges; server-written `summary` and all unknown document fields round-trip untouched (A3 open q. 8). Autosave posts the whole submission JSON as `answer.py` on every change while attempting (400 ms rate-limit on text inputs), with the QUIZ's ids overriding the base payload (`ApiClient.saveFile` gained `overrides`); submit = `updateSubmission {status: 0, correct: false}` → server `process_quiz` feedbacks applied (incl. the Generic-LTI-Failure quirk), `attempting` mirrors the server's rewrite, `markCorrect` on the echo.
- [x] Hidden-pool answers preserved (D7-B, **LD-7 — resolved & revised**): the merge-into-studentAnswers plan would have CHANGED scores (`process_quiz` grades every answered question, quizzes.py:72-79); they persist under the additive server-ignored `hiddenAnswers` key instead, restored to `studentAnswers` when the question re-pools visible.
- [~] Pyodide preprocessing (§6.5, fail-soft `preprocessing_error`, ships flagged) — **deferred**: new additive capability, no legacy counterpart (A3 §6); schema reserves nothing for it, so it can land independently. Instructor authoring modes (RAW/FORM/QUIZ_EDITOR textareas, inline body markdowneditor) also deferred with the assignment-editor scope (README §11.3.7 keeps the quiz editor out of v1); string `readingId` slug lookup wired as an optional prop pending an id-by-url endpoint.
- [x] **Tests (§16.1.5):** 20 quizzer unit tests (fill-ins/backfill defaults, seeded pools + reproducibility, blank grammar, per-type answers, LD-7 stash/restore, attempt bars, autosave ids, submit→feedbacks→markCorrect, NONE hiding, instructor view) + the smoke test driving the full lifecycle against the dev stub: dispatch subordinate quiz 102 → Start → 2 fixed + 1-of-2 pooled questions → radio answer autosaves the JSON document → Submit posts `status=0&correct=false` → graded feedback boxes render → nav numerator bumps with no ✔ (the A7 §2 unknown-option quirk).

### Milestone 2.4b — Visual quiz editor (new requirement, 2026-07-11) — **landed 2026-07-11**

New scope (user requirement; the legacy QUIZ_EDITOR mode was never built). Latest-engine field reference: `C:\Users\acbar\Projects\cisc108\bakery-exams\src\formats\quizzes` (quiz_check.py read 2026-07-11).

- [x] **Local grading engine** (`quizzer/grading.ts`): TS port of the server's `process_quiz`/`check_quiz_question` (quizzes.py:44-228), quirk-exact — `str(bool).lower()` true/false compare, matching zip + alternative-list entries + BOTH feedback shapes (server statement-list and bakery statement-dict), multiple-answers set-equality + per-option `wrong`, trimmed-submission `compare_string_equality`, the regex-feedback-keyed-by-correct_regex quirk, unanswered-question skipping, `all([])`-is-true matching edge. Fill-in rich feedback implements the DOCUMENTED grammar (string | {regex: msg} | [{regex: msg}]) — the server's own iteration is broken (quizzes.py:202); the validator warns about the divergence. `correct_any` (bakery FEEDBACK_FIELDS) is preserved but ungraded, matching the server.
- [x] **Validation engine** (`quizzer/validation.ts`): port of bakery's `IssueTracker` — required/typed main fields, learning-objective alias fields, retainOrder/statements/answers type+scope rules, per-type correct/feedback cross-referencing (answers membership, statement counts, blank-id coverage both directions), the `[a-zA-Z0-9_-]` answer-id charset, regex compilation checks, error/warning severities — plus orphaned-check and pool-reference/starvation checks.
- [x] **QuizEditor** (`quizzer/editor/QuizEditor.tsx`): instructor authoring surface with four modes — **Visual** (settings form incl. pools; per-question `QuestionEditor` cards with type-specific widgets for all 10 types, Advanced-JSON escape hatch for rich shapes so every latest-engine field is reachable, add/delete/reorder/rename with checks+pools following, live per-question issues + toolbar issue count), **Raw** (the two document strings verbatim — the legacy RAW fallback), **JSON** (pretty-printed with live parse errors — the legacy FORM fallback), **Try It** (the REAL student `Quizzer` running against the unsaved draft; grade **locally** via the engine port for instant feedback on drafts, or **remotely** through the real saveFile+updateSubmission path against the last-saved documents, with an unsaved-changes warning). Per-question preview grades a scratch answer locally inside the editor card. Canonical state = the two raw strings, so unknown fields survive every mode.
- [x] **Wiring**: instructors default to the Quiz Editor view with an "Actual Quiz" toggle (the new normal workflow); `QuizzerAssignment` carries `onRun` (checks — server blanks it for students); `saveQuizAssignment` persists `!instructions.md` + `!on_run.py` through saveFile with the quiz's ids (legacy quizzer.ts:195-205) and reloads on success.
- [x] **Tests:** +20 (grading conformance incl. every per-type branch and the processQuiz totals; validation positives/negatives; editor flows: edit→save round-trip, live issues, add/delete sync, Raw/JSON fallbacks, Try It local-grading end-to-end). Suite: 487 unit / 15 gated smoke green.
- Deferred: markdown WYSIWYG for bodies (plain textarea + preview for now); `examples` field verification UI (validator-ready, no widget); assignment-settings (points/name/url) editing alongside the two documents.

### Milestone 2.5 — Composition, LTI, legacy islands — **landed 2026-07-11**

- [x] **`AssignmentSurface` context (§12)** — `editor/src/surface.tsx` (lives in `@blockpy/editor` since the dep chain is quizzer → reader → editor): `{assignmentId, submissionId, variant, depth, logEvent, runController}` with `'full' | 'embedded' | 'minified'` variants; omitted fields inherit from the enclosing surface, so a minified runnable attributes events to its hosting reading. Reader/Quizzer/Textbook each provide their OWN surface (Reader: `embedded` when `asPreamble`); RunnableBlock wraps its MinifiedEditor in a `minified` child. Depth guard: > 3 refuses to render children with a console warning (deepest legitimate chain = group → quiz → preamble reading → minified editors). **Owning-id attribution bug fixed en route:** the app's shared surface-id slots would cross-attribute events when a quiz and its preamble reading mount together (whichever pair loaded last claimed BOTH surfaces' events); each requested id now keeps its own slot (`makeSurfaceLogEvent(requestedId)`).
- [x] **`@blockpy/lti-embed` (§13)** — ports of the editor.html/textbook.html inline scripts: `installFrameResize` (JSON-stringified `lti.frameResize` height+50 to parent origin `'*'`, install + body ResizeObserver debounced 500 ms; caller owns the `embed` gating — note textbook.html:213 gates it INVERTED `{% if not embed %}`), `installCookieFallback` (`checkCookies` core.ts port, `window.ltiLoadedCorrectly`, verbatim console error, the put_data state+nonce handshake with the legacy validation ladder — generated-UUID substitution is **ledger LD-14**; `PLATFORM_ORIGIN = '*'` kept behind a constant incl. its unreachable-response consequence), `loadingNoticeHtml`/`removeLoadingScreen` (the verbatim Safari notice + `.delete-on-load` removal), `emojiProxyUrl` (editor.html:294 mapping). **Emoji finding (2026-07-11):** the ONLY legacy consumer of `Sk.emojiProxy` is pygame4skulpt's `image.load` (emoji-SVG pygame surfaces) — NOT output rendering; §13's "output rendering" phrasing is superseded, and the mapping ships as config plumbing (`paths.emojiProxy`) for a future pygame-equivalent. App wiring: cookie fallback + loading-screen removal at mount (guarded by `ltiLoadedCorrectly === undefined` so shim pages that ran the template script don't double-handshake), frame resize while `display.embed`.
- [x] **`legacy-shim` completes (§15.3)** — `installFrontendStub` publishes `window.frontend = {checkCookies, generateUUID, Server}` only when the real bundle is absent; `Server` is constructor-compatible with services/server.ts:44 plus the `makeBlockPyRequest` timestamp/timezone stamping. Navigation compatibility globals (`URL_MAP`/`INDICES`/`FIRST_ID`/`LAST_ID`/`FULL_SELECTOR_DIV`/`loadNavigation()`) live in `@blockpy/navigation`'s `publishNavigationGlobals` — published by the APP when it owns the navigation, each key only if currently undefined (template-macro definitions always win), disposer removes only what it set; documented deprecated.
- [x] **Kettle/Explain legacy islands (§17)** — `app/src/LegacyIsland.tsx`: renders the legacy custom element (`<kettle params=…>`, editor.html:168-185 model slice with `ko.observable` currentAssignmentId) and `ko.applyBindings` when `window.ko` has the component registered + `frontend.Server` exists; `ko.cleanNode` on unmount; graceful notice otherwise (smoke-tested on the dev harness, which ships no bundle). The islands' `$MAIN_BLOCKPY_EDITOR.components.server` surface (createServerData/`_postBlocking`/`_postRetry`/`altLogEntry`, kettle.ts:1003/explain.ts:274) plus `model.display.passcode()` is grafted via `createLegacyServerBridge` delegating to the api client/transport — never clobbering a real editor's own surfaces.
- [x] **Textbook decision → PORT as thin `reader` composition (§11.4)** — parity reference is the WORKING standalone textbook.html page, NOT the `<textbook>` ko component (unfinished, crashes in the group path — **ledger LD-15**). `@blockpy/textbook`: v1 document parser (textbook.py port incl. the version guard and MISSING_READING), sidebar macro (indent `5+indent*8`px, info accent at indent 0 only, disabled-secondary headers), first-reading default page, `?page=` pushState/popstate/title contract, pages render through the injected full `Reader` (embedded surface, NO-OP markCorrect per textbook.html:109 — readings still post their own markRead; the textbook itself never marks correct, its markRead is commented out in legacy), instructor RAW editor (instructions+settings textareas → saveFile `!instructions.md` + saveAssignment settings; FORM mode deferred). Client-side rehydration gap is **ledger LD-16** (server-team flag: rehydrated load_assignment or a by-url endpoint; the parser already accepts both shapes and the dev harness serves the rehydrated form).
- [x] **Tests:** +49 unit (surface context 6, lti-embed 12, navigation globals 4, frontend stub 4, textbook document 8 + component 8, legacy island + bridge 8, existing suites re-verified) → **536 passed / 12 skipped**; +3 smoke (textbook composition end-to-end incl. markRead attribution and the RAW editor, kettle island degradation, LTI page environment incl. the §15.3 globals) → **18 non-gated / 19 with `PYODIDE_E2E=1`, all green**. Harness: textbook 105 serves a real chaptered document over reading 103, kettle 108 demos the island notice, the page ships a legacy `.delete-on-load` span.
- Deferred within M2.5: standalone textbook-page ownership (the dedicated route with server-rendered sidebar stays legacy — now scheduled as Milestone 4.7); running Kettle/Explain against the REAL bundle end-to-end (needs a page that loads it — shim-mode verification when a flagged course exercises it); `X-` event wiring for textbook page opens.

**Exit criteria:** golden-transcript replay passes for the full scripted session (the primary G3 gate); navigation + quiz Playwright suites green; a flagged course runs a mixed group (coding + reading + quiz) end-to-end in an LMS iframe.

---

## 4. Phase 3 — Punch list: bug burn-down & parity gaps (maintainer review, 2026-07-11)

Goal: burn down the defects and gaps from the maintainer's first full review before layering new capabilities. Every item was investigated against the code on 2026-07-11/12 (five parallel deep-dives: engine/Python-strings, editor chrome, block editor, quiz/files/textbook, legacy server references); findings and exact fix sites are inline. Ordering is severity × dependency: block mode is unusable without M3.1, grading correctness (M3.2) affects every course, and the Settings editor (M3.5) is the home for the new settings keys other milestones introduce.

Ledger entries pre-assigned here: **LD-17** (fullscreen failure event), **LD-18** (share prompt only on negative rating), **LD-19** (badges for previously blank categories), **LD-20** (console image sizing), **LD-21** (working file rename/delete/namespace UI). Note: grading-always-runs (M3.2) is NOT a ledger item — it restores legacy behavior Studio accidentally tightened.

### Milestone 3.0 — Python-source hygiene (quick win, do first) — **landed 2026-07-12**

Audit result (2026-07-11): exactly two production payloads live as TS template literals — `packages/engine/src/runtime.py.ts` (~340 lines of Python, consumed via `runner.ts:7` → `pyodide.runPython`) and `packages/engine/src/pedal-env.py.ts` (~115 lines, `pedal.ts:11`). Test snippets are a few lines each and stay inline.

- [x] Bodies moved to real `packages/engine/src/{runtime,pedal-env}.py` (extraction script eval'd the literals — byte-identical round trip verified — then the design-doc headers were re-added as Python comments); imports via Vite `?raw` with default-import re-exports in `index.ts` (public API unchanged); `packages/engine/src/raw.d.ts` declares `*.py?raw`, with triple-slash references at the two import sites so the ambient module travels into CROSS-PACKAGE programs (the app/legacy-shim typechecks compile engine sources through the source-first exports and don't see engine's own `include`).
- [x] `tools/run-grader-corpus.mjs` now `readFileSync`s the real `.py` (regex extraction deleted).
- [x] Escape hazards gone with the move; engine suite green (30 passed / 12 gated-skipped) + all package typechecks.

### Milestone 3.1 — Block editor unbreak (right-click, variables flyout) — **landed 2026-07-12**

Root cause found (2026-07-11): Studio imports `blockly/core` directly and never calls `Blockly.setLocale` — only the full `blockly` package entry does that — so `Blockly.Msg` is EMPTY. The default context-menu items (which ARE registered; the `registerDefaultOptions` hypothesis was ruled out) throw `TypeError` on `Msg.DELETE_X_BLOCKS.replace(...)` at right-click, and every Msg-driven label renders blank (the Variables flyout's "Create variable…" button, rename/delete labels, `astName.ts:85`'s delete-variable path).

- [x] `registry.ts` calls `Blockly.setLocale(En)` at bootstrap (`blockly/msg/en` named exports) — right-click crash + blank labels fixed in one move.
- [x] Variables flyout ported as `packages/blocks/src/variables-flyout.ts` (blockly_shims.js:64-119): `ast_Assign`/`ast_AugAssign` starters (legacy gaps/shadow/mutation), case-SENSITIVELY sorted `ast_Name` getters, `_HIDE_GETTERS_SETTERS` label fallback — the toolboxes.ts toggles live again. Blockly 11 captures `flyoutCategoryBlocks` as a module-LOCAL binding when registering the default VARIABLE callback, so the legacy monkey-patch route is dead — `installVariablesFlyout(workspace)` RE-registers the category callback instead (called after inject in `block-editor.ts`). One intent-fix documented in-code: legacy string-concatenated the raw VariableModel into the starter XML (an `[object Object]` text node), silently dropping the most-recent-variable field it plainly meant to carry — Studio emits the real field element.
- [x] Tests: `variables-flyout.test.ts` (5) — Msg population incl. the exact `DELETE_X_BLOCKS` template, ast_* composition, shadow/mutation fidelity, case-sensitive sort (`Zebra` < `apple`), hidden-mode labels. _Playwright right-click smoke rides the next e2e run._

### Milestone 3.2 — Grading resilience & console correctness — **landed 2026-07-12**

- [x] **Grading survives student errors (legacy-parity fix).** The adapter now chains the Pedal job on BOTH paths (engine-adapter.ts): whenever a non-empty `!on_run.py` exists and `disable_feedback` is off, the grading job runs — Pedal's own `set_source → run` captures the student's syntax/runtime error as feedback (Py3.14 SyntaxError fail-soft already in pedal-env) — while the student's raw traceback STILL reaches the student console (`studentError` overrides the graded outcome's error field). The hand-built runtime/syntax feedback survives only as the no-grader/disabled fallback. `disable_feedback` plumbed for the first time: settings blob → App → new `CodingEditor.disableFeedback` prop → `RunOptions.disableFeedback` → adapter. Grader stdout/stderr stays on the dev console. Tests: `engine-adapter.test.ts` (4, mocked EngineClient) — error+grader → both jobs + Pedal feedback + traceback preserved; disable_feedback → single job + fallback feedback; clean-run paths unchanged.
- [x] **Traceback formatting.** Console stderr renders as `<pre className="blockpy-printer-traceback">` (Console.tsx; DevConsole too — grader tracebacks land there) with mono/`pre-wrap`/darkred rules in blockpy.css (+ maintainer-added red left border on traceback rows).
- [x] **Console images (ledger LD-20).** `width: 100%; height: auto; max-height: 480px; object-fit: contain` replaces the legacy 100px thumbnail rule; ledger entry written.
- [x] **Badge coverage (ledger LD-19).** Verified against the local Pedal 3 source (pedal/core/feedback_category.py): TIFA feedback is literally **`algorithmic`** — the unmapped literal behind the reported missing badge. `categories.ts` now maps the full Pedal 3 set: `algorithmic` → Algorithm Error, plus `mistakes`/`specification`/`style`/`system`/`positive`/`student`/`uncategorized` on the existing legacy color hooks. Ledger entry written.
- [x] **View-Trace header gap.** Feedback/Trace/Console/DevConsole headers converted from clearfix+float to a shared `.blockpy-panel-header` flex row (action buttons pushed right via `margin-left: auto`) — the floated `btn-sm` no longer inflates the row above the baseline-seated title.

### Milestone 3.3 — Chrome interaction fixes — **landed 2026-07-12** (block-sync debounce included)

- [x] **"Add New" layout.** Correction to the investigation: the dropdown rules DID exist in bootstrap-subset.css — the real gap was the Make-New-File dialog's grid: `col-sm-2/4/10` and `.text-right`/`.mt-2` were never pinned into the subset, so the form's labels/inputs rendered as unsized flex children. Added the BS4 col-sm block + utilities; the dialog's instructor intro paragraphs got their missing `col-sm-12`.
- [x] **Share link button.** The M1.6 wiring was already faithful (gated on `config.urls.shareUrl`, which real editor.html pages ALWAYS inject — editor.html:219); the button was missing only in the dev harness, whose BootConfig omitted the key. Harness now ships a `shareUrl`, matching every real page.
- [x] **Fullscreen failure logging (ledger LD-17).** Two-arm `.then(onSuccess, onFailure)` in `toggleFullscreen` — Success never fires on a rejection; failures log only the (already-registered) `X-Display.Fullscreen.Error`. No new registry id needed; ledger entry written.
- [x] **Share prompt only on negative rating (ledger LD-18).** `Feedback.rate` triggers the prompted share only for `thumbs-down`; thumbs-up ends at the thank-you. Test updated + a new thumbs-down case; ledger entry written.
- [x] **Autocomplete default OFF + toggle.** `autocompletion()` wrapped in a CM6 Compartment defaulting to `[]`; `setAutocomplete` through `DualTextEditor`/`DualEditor`; chrome-store flag persisted to `BLOCKPY_display.autocomplete`; PythonToolbar gained the toggle button (Lucide WandSparkles, rationale in icons.tsx); CodingEditor applies the persisted preference on editor mount and live on toggle.
- [x] **Editor responsiveness (from the M3.2/J finding).** `blockDelay` defaults to 300 ms — but ONLY the text→blocks regeneration debounces; the code mirror + change listener now fire immediately in `handleTextChanged` (Run/autosave must never see stale text — this also fixes a latent stale-Run hazard the legacy-shaped debounce would have had). Text mode was already free (the hidden block editor defers via `outOfDate_`). Highlighting untouched (it was never delayed). Default pinned in dual-editor.test.ts.

### Milestone 3.4 — Quiz presentation fixes — **landed 2026-07-12**

Root cause (2026-07-11): the `box-sizing: border-box` reset is deliberately scoped to `.blockpy-content` (bootstrap-subset.css:23-28), and the quiz surface never renders inside one (`AssignmentSurface` emits no DOM wrapper), so every `.form-control` (`width: 100%` + padding + border, resolved as content-box) overflows its column. The quizzer ships no CSS of its own.

- [x] New `packages/quizzer/src/styles/quizzer.css` (package `./styles/*` export added; imported in App.tsx with the other surfaces): question-card-scoped border-box reset — quizzes deliberately NOT wrapped in `.blockpy-content`.
- [x] Inline fill-in blanks: `.quizzer-inline-blank` (inline-block, width auto, min-width 8ch, prose-sized) layered over `.form-control` on the portaled inputs; `.quizzer-inline-select` defined; slot spans carry `.quizzer-blank-slot`.
- [x] Overflow: short-answer/numerical covered by the reset; essay textareas capped with `max-width: 100%`. _(The Textbook RAW-editor textareas live outside the card scope — covered when the card reset generalizes or M4.6 reworks that screen; noted, low-risk.)_

### Milestone 3.5 — Assignment Settings editor + new settings keys — **landed 2026-07-12**

- [x] **SettingsEditor component (the empty-Settings-tab fix).** `chrome/SettingsEditor.tsx` — the ASSIGNMENT_SETTINGS_EDITOR_HTML port: the full legacy boolean table (all 29 rows incl. the INCOMPLETE-marked ones, wire keys + docs verbatim), Passcode/Datasets/Preloaded-Files/Part-ID strings, Block Toolbox + Problem Type selects, Starting View radios, and (when the host passes them) the assignment columns Name/URL/Points/IP-Ranges + Public/Hidden/Reviewed. Canonical state is the raw blob: Save merges ONLY edited keys over the original object (unknown keys round-trip, D5-B/LD-5; unedited defaults don't pollute the blob), with a raw-JSON escape hatch that overrides the form and rejects invalid JSON. Dispatch branch beside the ImagesManager case in CodingEditor; App wiring prefills columns from the decoded assignment, live-applies saves by updating `loaded.assignment.settings` (the settings memo re-derives — toolbox etc. reload immediately), writes the VFS file, and POSTs `save_assignment` (settings + edited columns) when connected. 4 component tests.
- [x] **`allow_real_requests` setting (default false = today's mock).** Plumbed end-to-end: settings blob → App (`settingBool`) → `CodingEditor.allowRealRequests` → `RunOptions` → `EngineJob.allowRealRequests` (protocol) → `JobRunner` → Python `StudioRuntime.run(..., allow_real_requests)` which skips `install_requests_mock` when true. The runner lazily installs `requests` + `pyodide-http` (micropip, once) and calls `pyodide_http.patch_all()` — fail-soft when offline (`import requests` then raises Python's own ModuleNotFoundError). Site-packages adoption in the per-job restore keeps the real package resident; mock runs still purge their dynamic module, so the two modes can alternate. Key name `allow_real_requests` follows the A4 snake_case convention; surfaced as a Studio-marked checkbox in the new form (CORS best-effort documented, risk R9).
- [x] Studio-new keys register in the same form as they land (`allow_real_requests` shipped; `docs_url` joins with M4.3); autocomplete stayed a per-user display toggle (M3.3), not an assignment setting.

### Milestone 3.6 — Modern Python syntax in blocks (`match` first) — **landed 2026-07-12 (in full)**

The grammar already parses `match`/`case` (@lezer/python 1.1.19 has `MatchStatement`); the gap is Studio-side. Two fallback layers matter: a missing `to-ast.ts` case ABORTS the parse and rawifies everything from that line to EOF (worst); a missing converter rawifies just the one statement.

- [x] **match/case (required) — landed 2026-07-12.** `ir.Match`/`ir.MatchCase` (patterns TEXTUAL v1 per the design decision — raw source between `case` and the clause body, guards included); `to-ast.ts matchStatement` (subject via expressionSequence — tuple subjects work; per-clause pattern slice + body); `recursiveMeasure` grew a `cases` branch so case-body statement heights align with convertBody's shifts; `ast/astMatch.ts` (ast_If idiom: SUBJECT value input, per-case dummy header with an editable pattern `FieldTextInput` + statement input, case count in the mutation; generator emits `case`-lines at INDENT with `prefixLines`-deepened bodies); toolbox entry in the `full` preset ONLY (legacy presets are B6 parity fixtures and stay verbatim). Tests: 5-case round-trip suite (literal/wildcard, guards + or/class/as-patterns, nested-in-def with successor statements, multi-statement bodies, and the no-rawify-after-match regression) — exact-text double-trip, corpus untouched (131 blocks tests green). **Found en route:** @lezer/python 1.1.19 REJECTS empty class patterns (`case str():`, `case int() | float():` — probed 2026-07-12); an upstream grammar gap in the S2 valueless-`yield` family — such programs degrade to ast_Raw; file upstream alongside the yield fix.
- [x] **Cheap wins — landed 2026-07-12:** `astAwait.ts` (await value block), `astBytes.ts` (original literal text preserved, like Num's `source`), `astEllipsis.ts`, and `astNamedExpr.ts` + the `NamedExpression` to-ast case (walrus was a parse-level whole-remainder rawifier; the block reports lowest precedence so `if (n := f()) > 4:` re-parenthesizes exactly). 8-test round-trip suite (`modern-syntax.test.ts`).
- [x] **async correctness — landed 2026-07-12 (raw-fallback design, revised from the flag-on-blocks plan):** the leading `async` keyword is stripped in `functionDefinition`/`forStatement`/`withStatement` (the `children[1]` name read grabbed `def`; `async for/with` choked expressionSequence on the shifted keyword) and recorded as `is_async` on the IR; the FunctionDef/For/With converters THROW on it, riding convertStatement's established per-statement `ast_Raw` fallback — async constructs preserve their text verbatim as a single raw block (siblings unaffected) instead of corrupt blocks or whole-file rawification. A real async block form (mutation flag on the three legacy-ported modules) is deliberately deferred until curriculum needs it.
- [x] Documented-acceptable raw fallbacks: `except*`, PEP 695 `type`/type-params, and (found en route) @lezer/python 1.1.19's rejection of EMPTY class patterns (`case str():`) — upstream grammar gaps in the S2 valueless-`yield` family.

### Milestone 3.7 — File management controls & filesystem tree — **landed 2026-07-12**

- [x] **VFS:** `rename()` + `changeSpace()` with the legacy magic-name guards (`canRenameName`/`canDeleteName` capability helpers mirror UNRENAMABLE_FILES/DELETABLE_SIMPLE_FILES, files.js:229/234); refuse clobbering and renaming ONTO magic names; write+delete change events keep the file UI live. Wire semantics pinned in the LD-21 ledger entry: rename/move are client-side — the file persists under its new name through the normal save channels; no server rename endpoint exists (server-team flag noted).
- [x] **Buttons (ledger LD-21 — written).** PythonToolbar gained a Rename/Delete group for the ACTIVE file (enabled per VFS capability × role editability); prompts follow the legacy plain-prompt convention. New `X-File.Rename`/`X-File.Move`/`X-File.Delete` extension events at the handler sites; deleting the active file falls back to answer.py. Namespace-move is instructor-only (net-new) via a prefix prompt (`!`/`?`/`&`/`^`/blank).
- [x] **Filesystem tree sidebar** — `chrome/FileTree.tsx`: collapsible left rail (store `fileTree`, default OFF, persisted `BLOCKPY_display.fileTree`; toggle in the tab strip + a hide button in the rail header), bucketing `vfs.listVisible(role)` by namespace with the permissions matrix intact; instructor view shows prefix chips + full legacy names, students see friendly bucket labels + basenames; rows carry the LD-21 actions (hover-revealed, capability-gated), `&`-space rows keep the legacy uneditable look. Layout: rail = `col-md-3` beside a shrunk `col-md-9` editor column; in TEXT-only mode the tree REPLACES the horizontal tab strip. Gated like the files UI (`instructor || !hideFiles`). Tests: 3 VFS suites (rename/move/capabilities) + 4 FileTree component tests (buckets/visibility, instructor prefixes, action gating incl. magic-name suppression + stopPropagation, live VFS re-render). _The rail-column grid generalization is the layout groundwork for the M4.3 docs panel._

---

## 5. Phase 4 — Studio extensions (new capabilities, maintainer 2026-07-11)

All additive with no legacy analog (or explicitly superseding a legacy screen); each ships opt-in per the §17.5 discipline, with defaults preserving the B6 visual-parity mandate.

### Milestone 4.1 — Theme system: light (default) / dark / Windows-2000 ✅ (landed 2026-07-12, LD-23)

- [x] Mechanism: `styles/themes.css` adds `[data-theme="dark"]` / `[data-theme="win2000"]` scopes overriding the tokens; unthemed = today's values. Light stays the parity default; themes are explicit user opt-in (LD-23).
- [x] Dark beyond tokens: CM6 dark HighlightStyle + `EditorView.theme({}, {dark:true})` via a theme Compartment (`DualTextEditor.setTheme`); Blockly `blockpyDark` componentStyles theme (`DualBlockEditor.setTheme`, workspace live-swaps); badge/accent contrast overrides (darkred/black/offline badges, positive-green/alert-red text accents) — full AA audit rides the Phase 6 pass. Every `DualEditorView` follows the store, so minified editors theme too.
- [x] Windows-2000: cosmetic skin only — `#c0c0c0` surfaces, box-shadow bevels (NOT border-width changes, keeping metrics identical), square corners, Tahoma/MS-Sans-Serif stack, navy title-bar dialogs.
- [x] Palette button in the quick menu cycles light→dark→win2000; persisted (`BLOCKPY_display.theme`); `prefers-color-scheme` ignored — parity default until the user chooses.

### Milestone 4.2 — Focused editor mode (exam-friendly) ✅ (landed 2026-07-12, LD-24)

- [x] `focusedMode` store flag (not persisted — loads start in parity chrome): instructions pane, quick menu, file strip/tree, and App's group-nav headers hide; toolbar stays; editor col-md-12 and remounts taller (`innerHeight - 220`, floor 500); console + feedback render in a collapsible bottom drawer with the `label-*` feedback badge always visible; Instructions reachable via a Dialog overlay toggle in the drawer bar.
- [x] Toolbar Focus button, `Esc` leaves, `Ctrl+Alt+F` toggles; browser fullscreen composes (independent state, QuickMenu path untouched).
- [x] `X-Display.Focus.Enter`/`.Exit` logged on user enter/exit (assignment switch restores silently) — registered in A2 §5.1 (new Studio-extensions table, which also back-fills the M3.7 `X-File.*` events).
- [x] Playwright (smoke.spec): enter → CM surface grows and rises; drawer expands from the badge; instructions overlay opens; Esc restores; run cycle inside focus mode keeps state. Unit: CodingEditor focused-mode + keyboard tests.

### Milestone 4.3 — Docs browser (right-hand panel) ✅ (landed 2026-07-12, LD-25)

- [x] `DocsPanel` in a col-md-3 right rail beside the editor; source = new `docs_url` setting (raw string, A4 Studio-extensions table + SettingsEditor string row); fetched once per session per URL (module cache; failures don't poison it); rendered through the A6 marked + hljs pipeline (`renderInstructions` + 400 ms highlight).
- [x] TOC from headings (post-render id assignment, level-indented, scroll anchors) + text filter box; expand state persisted (`BLOCKPY_display.docsPanel`); explicit download link. Grid negotiation: editor is `12 − 3·(tree) − 3·(docs)`; hidden in focused mode. Toolbar "Docs" toggle renders only when the setting exists.

### Milestone 4.4 — CSV & JSON editors ✅ (landed 2026-07-12, LD-26)

- [x] Extension dispatch in the tab body: `.csv` → `CsvEditor` grid (chrome/csv.ts RFC-4180-ish parse/serialize, header toggle, add/remove rows/columns, cell edits through handleCodeChange so VFS/autosave/dirty just work); `.json` → `JsonEditor` (CM6 lang-json + jsonParseLinter gutter + synchronous validity badge + collapsible tree view).
- [x] "Raw Text" escape with a "Back to Grid/JSON Editor" return (reset on file switch); unparseable CSV (unclosed quote → `parseCsv` null) degrades to the text editor; `&`-space read-only rides `fileReadOnly` (D3-A).

### Milestone 4.5 — Image preview & pixel editor ✅ (landed 2026-07-12, LD-27)

- [x] Graphic extensions (.png/.jpg/.jpeg/.gif/.bmp) dispatch to `ImageEditor`: checkerboard backdrop, 0.5–8× zoom, natural-dimensions readout; raw-text escape/return matches M4.4.
- [x] Pixel-grid editor for sprite-scale images (≤ 64×64): palette + custom color + eraser, click/drag paint, resize preserving top-left, blank-canvas creator for fresh files; Apply re-encodes to a PNG data-URL. Pure grid model (`pixel-grid.ts`) unit-tests without a canvas backend. **Storage decision (task closed):** editable representation = VFS text contents as a data-URL (normal save channels); uploads stay ImagesManager-only (preview/replace, no pixel editing).

### Milestone 4.6 — Assignment-group organizer (instructor)

Reference investigated 2026-07-11: `courses/edit_settings.html` is a bulk FORM, not a drag-drop UI — per-assignment name/url + public/hidden/reviewed/subordinate booleans, per-group name/url; `subordinate` is a plain boolean (no parent pointer exists); server `position` columns exist but ordering-by-position is commented out (course.py:204-244). Studio builds the group-scoped equivalent, not a port.

- [x] **Slice 1 (existing endpoints)** ✅ (landed 2026-07-12, LD-28): `GroupOrganizer` dialog from the instructor bar — group rename/url (`editAssignmentGroup`), per-assignment name/url/points/public/hidden/reviewed with touched-fields-only saves (`saveAssignment`), remove/add via `moveMembership` (`new_group_id=-1` removes). Nav header refreshes in place (`GroupNavStore.renameEntry`/`removeEntry`). The two `/assignment_group/*` URLs are NEW `$blockPyUrls` keys — capability-detected until templates publish them (server-team flag).
- [ ] **Slice 2 (server-team flags, LD-16 style):** true reordering (pass `position` through move_membership — the route currently drops it — and re-enable the position `order_by`), change assignment `type` (NO endpoint exists today; server must extend `save_assignment`), `subordinate` toggle via JSON (today bulk-form-only). Client ships capability-detected: controls appear when the server accepts them.

### Milestone 4.7 — Textbook URL routes (supersedes the M2.5 deferral) ✅ (landed 2026-07-12; LD-16 closed)

- [x] **LD-16 closed:** `ApiClient.loadAssignmentByUrl` over the GET-only `/assignments/by_url` route (new `Transport.getJson` — the only GET in the client; fail-soft null) wired into App's `<Textbook resolveAssignment>`. Capability-detected via the new `loadAssignmentByUrl` `$blockPyUrls` key (server-team ask shrinks to publishing it).
- [x] **Standalone route:** BootConfig `assignment.textbookPath` entry mode (used only when `currentAssignmentId` is null): resolve `<path>` by url THEN numeric id, dispatch through the host; boot spinner covers the resolution; unresolvable paths surface the load error. The initial `?page=` was already honored in-component (Textbook.tsx pageParam, url-then-id). Server side stays a flagged template swap.

---

## 6. Phase 5 — Code quality & standards (parallel workstream; starts immediately)

Lint baseline (2026-07-11, `pnpm lint`): **136 problems** — 72 `@typescript-eslint/no-explicit-any` (error via tseslint recommended), 45 `no-unused-vars` (dominated by `_parent` params; the `_` prefix isn't configured as ignorable), 9 `react-hooks/exhaustive-deps` warnings (CodingEditor ×6, MinifiedEditor, QuestionView, QuizEditor — all the missing-`props` pattern), 3 stale eslint-disables, 6 misc errors. `any` is concentrated in `packages/blocks` (astCall 24, astComp 17, astFunctionDef 11, text-to-blocks 8); editor/quizzer are mostly clean.

- [x] **M5.1 — Lint to zero (non-`any`)** ✅ (landed 2026-07-12): stale disables dropped (DualEditorView, quizzer `complexity` pair); prefer-const / no-useless-escape fixed; `no-this-alias` sites became lexical arrows (astAugAssign, astCall context menu); the verbatim-legacy `STRING_IMAGE_URL` regex keeps its dangling-`\2` quirk under a justified block disable; all 9 exhaustive-deps warnings resolved via the destructure-props pattern — which surfaced and fixed a REAL staleness bug (handleRun's deps were missing `disableTifa`/`disableInstructorRun`/`seed`, so mid-session settings changes never reached the run). `pnpm lint` now runs `--max-warnings 0`; CI already fails on it.
- [x] **M5.2 — `any` elimination in `packages/blocks`** ✅ (landed 2026-07-12, 72 → 0): generic `Converter<N extends AnyNode>` with `ConverterParent = AnyNode | undefined` (verified against what `convert()` actually passes); new `Parented` interface types the `_parent` backlink the doc-string probe reads; `AnyDuringMigration` deleted (Blockly 11 publics cover most of it); `ORDER_RELATIONAL` joined the generator's deprecated-alias surface. Five `as unknown as` casts remain, each commented (protected `definitions_`, pre-v11 `mutator.isVisible`, removed `Blockly.Constants.Variables`, astWith mutation array, `bumpNeighbours_`). Rule stays at error; round-trip corpus 139/139 green.
- [x] **M5.3 — `docs/CODE_STANDARDS.md`** ✅ (landed 2026-07-12): no-`any` policy with the ordered escape-hatch ladder (precise type → unknown → justified `as unknown as` → justified disable), hooks-deps destructure/ref-mirror patterns, `_`-prefix convention, module layout (one legacy file ↔ one module; appendix citations), comment policy (constraints only, ledger citations), test conventions (conformance vs unit; jsdom `afterEach(cleanup)`; PEDAL_IT/PYODIDE_E2E gates), ledger discipline, and the PR checklist. Referenced from the README header.

---

## 7. Phase 6 — Hardening & Default-On (spec §17 Phase 3)

- [x] **M6.1 — WCAG 2.1 AA automated audit + fixes** ✅ (landed 2026-07-13, LD-29): `e2e/a11y.spec.ts` runs axe-core (wcag2a/aa/21a/21aa) over the editor+nav, quiz, reading, and textbook surfaces as a standing e2e gate (Blockly SVG excluded — keyboard-nav plugin territory). Fixed: quick-menu role menubar→toolbar; file strip's incomplete tab ARIA replaced with a plain nav list + `aria-current`; CM6 `aria-label`s; accessible names for icon buttons, the assignment selector, and every quiz input; contrast fixes for text-muted/btn-outline-secondary/btn-success/textbook-active/footer badges (ratios in LD-29). **Residual:** feedback `label-*` badge colors (A8-normative, still sub-AA when shown), keyboard-only/manual passes.
- [ ] **M6.2 — Remaining §16.3 non-functional acceptance** _(first slice landed 2026-07-13)_:
  - [x] **Blockly keyboard-nav plugin** (LD-30): @blockly/keyboard-navigation 0.6.14, page-global controller, persisted default-off toolbar toggle; disposed editors deregister. Found + worked around an upstream Blockly 11.2 leak (trashcan flyout workspace survives `WorkspaceSvg.dispose` — one retained registry entry per editor mount).
  - [x] **Memory budget legs:** one engine per page is by construction (the App's single RunController is handed to every editor; PYODIDE_E2E smoke exercises the shared engine); 10-editor mount/unmount releases every Blockly workspace (MinifiedEditor unit test); `e2e/perf.spec.ts` heap canary — editor + hydrated reading measured at ~34 MB against the agreed 150 MB pre-engine budget.
  - [x] **Engine lazy-load:** already true (R7 design; "Run boots the engine lazily" smoke test is the verification). **Caching posture documented:** `paths.pyodideIndexURL` is deployment-controlled — production points at a versioned CDN/immutable path so wasm + wheels ride HTTP caching; a service worker stays deferred until real-course telemetry says otherwise.
  - [ ] Safari compat-mode verification (manual/external — needs a Safari machine; compat mode is already the primary path per R1).
  - [ ] i18n externalization (all UI strings) — large; Blockly locale wiring already exists (`setLocale(En)`, registry.ts).
- [ ] **M6.3 — Finish the Skulpt-compat instructor appendix (§6.7):** needs the real-student-submission corpus (§16.1.3) for the float-repr/error-wording/time deltas.
- [x] **M6.4a — Public GitHub Pages demo** ✅ (landed 2026-07-13): `.github/workflows/pages.yml` deploys the dev harness statically (enable via repo Settings → Pages → Source: GitHub Actions). The harness gained an assignment-group dropdown (top bar): the showcase fixtures plus REAL bakery curriculum groups **1A) Introduction** (25 assignments) and **6B) For Loops** (12) — extracted from the untracked `courses/bakery_course.json` by `tools/extract-demo-groups.mjs` into the committed `packages/app/src/demo/bakery-groups.json`. The `/api/*` fixtures moved to the shared `src/dev-stub.ts`, consumed by the vite middleware (dev/e2e — real HTTP) and by an in-browser fetch stub (static builds); bakery quizzes grade locally through the quizzer's own `processQuiz` (ssr-loaded in dev, static import in the browser). Python/Pedal run in the visitor's browser via the Pyodide CDN. Fixed along the way: module workers need `worker.format: 'es'` (the app `vite build` had been broken). Follow-up (2026-07-13): quizzes now resolve url-slug `settings.readingId` through the GET-only `/assignments/by_url` route (`lookupReadingId` wired in App.tsx; the route stubbed in dev-stub + the vite middleware accepts GET) so subordinate readings render in full above the quiz for students; instructor views get a Show/Hide Subordinate Reading toggle, expanded by default (LD-31). The dev-shell chrome (harness header line + demo group picker) is gated on the Studio-only `display.devHarness` BootConfig flag, default off — real applications never see it; the harness page sets it and the published demo honors `?devharness=false`. Integration support (2026-07-13): instructor tools became icon buttons at the right of the top group-nav bar (LD-34), and BootConfig gained `paths.assets` (shim global `$blockPyAssetsPath`) pointing at the deployed `assets/` dir — the engine worker now builds as a STABLE `worker.entry.js` so that URL is constructible from config.
- [ ] **M6.4 — Default-on rollout:** legacy client installable for one semester; server templates shrink to BootConfig JSON block (§17 Phase 3); server-team flags land (`$blockPyUrls` keys for editAssignmentGroup/moveMembership/loadAssignmentByUrl; M4.6 slice-2 endpoints).
- [ ] **M6.5 — Flagged extensions as capacity allows (§17.5):** tightened postMessage origins, batched logging, `X-` events, wheel hosting (utility module + curriculum_ctvt/bakery_canvas bundling).

---

## 8. Phase 7 — Punch list 2: maintainer review of 2026-07-14

Goal: burn down the sixteen defects/gaps from the maintainer's 2026-07-14 review. Every item was investigated against the code on 2026-07-14 (five parallel deep-dives: feedback/error surfacing, engine-boot/toolbar/files, settings plumbing, blocks/quiz grading, UI/nav/fork); root causes and exact fix sites are inline below. Ordering is severity × dependency: grading correctness first (M7.0 — a blank T/F can record `correct: true`), then error-reporting correctness (M7.1), then the settings restorations (M7.2 — other milestones' gates ride on them), then chrome/UX, with the server-coupled fork flow last (its client work can't freeze until a server-contract check runs).

Ledger entries pre-assigned: **LD-35** (unanswered quiz questions graded as incorrect), **LD-36** (pink bug icon made real), **LD-37** (engine-boot indicator), **LD-38** (feedback rating pinned bottom-right), **LD-39** (upload without auto-run), **LD-40** (quiz font-size control), **LD-41** (textbook chapter expand/collapse), **LD-42** (working fork dialog). NOT ledgered: semicolon support, draggable dialogs, and the M7.2 settings keys (all restore legacy behavior); the `<exec>` strip (Studio-only runtime cleanup → skulpt-compat appendix); dark-nav theming and the focus-drawer default (amend LD-23/LD-24 in place).

### Milestone 7.0 — Quiz grading correctness (do first: wrong recorded grades) — **landed 2026-07-14** (client engine; server mirror = filed flag)

Root cause (2026-07-14): both graders silently skip questions whose answer is **absent** — the server's own documented "Hack" (`quizzes.py:72-76`) ported quirk-for-quirk (`packages/quizzer/src/grading.ts:328-330`). Skipped questions are excluded from `totalPoints` and never force `totalCorrect = false`, so a blank T/F rides an otherwise-correct submission to `correct: true`. Studio nuance: VISIBLE questions are pre-filled with `''` (`documents.ts:216-220`) and DO grade incorrect — the false-correct arises when the key is absent (pooled/hidden `hiddenAnswers` routing, partial submissions). Latent second divergence: the port's `String(student).toLowerCase()` T/F compare (`grading.ts:93-107`) turns undefined-student × unauthored-`correct` into `'undefined' === 'undefined'` → silent false pass where the server would crash on `None.lower()`.

- [x] **LD-35 (fix; server-team flag):** absent answers for PRESENTED questions grade as the type's EMPTY answer (`defaultAnswer(question, undefined)` normalization inside `checkQuizQuestion`) — incorrect, points counted, `totalCorrect = false`. Pool safety turned out to be the load-bearing subtlety (the server hack conveniently also skipped pooled-out questions): `processQuiz` gained `{visible?, seed?}` options — explicit set, or recomputed via `selectVisibleQuestions(seed, attempt.count)`; without context, non-pooled absent = graded, pooled absent = excluded (a `hiddenAnswers` stash is positive hiddenness evidence). Callers pass their seed: demo grader (`9000 + record.id` per `demoLoadResponse`), Try It (seed 0). Ledger written; server flag in the entry.
- [x] T/F branch hardened: unauthored `correct` NEVER grades correct; null/empty student never aliases via `String()` coercion ('undefined'==='undefined' was a silent false pass where the server crashes on `None.lower()`); `wrong` feedback falls back to "Incorrect" instead of stringified undefined. The top-of-function null-normalization covers every other branch's direct-call path (QuizEditor per-card preview included).
- [x] Visible-`''` vs absent split kept deliberate and documented in LD-35.
- [x] Tests: the skip test replaced with the LD-35 matrix (unanswered counted, pooled-out excluded with/without context, hiddenAnswers evidence, hardened-T/F cases) — 16 grading tests, 44 quizzer-wide green.

### Milestone 7.1 — Student tracebacks & internal-grading-error surfacing — **landed 2026-07-14**

- [x] **`<exec>` frame stripped from student tracebacks.** Root cause: `runtime.py` loads via `pyodide.runPython` with Pyodide's default `"<exec>"` filename (`runner.ts:119`), so every runtime error's traceback opens with the harness frame `File "<exec>", line 299, in run` (the `eval` at `runtime.py:299`) before the student's `answer.py` frame — and the string reaches the console verbatim (`CodingEditor.tsx:617` → `Console.tsx:103-105`). Landed as drop-ALL-leading-`<exec>`-frames (tb walk) PLUS a formatted-parts filter for non-leading harness frames — needed immediately, since `TraceLimitError` raises from the tracer at the TAIL of the chain. Regression test covers runtime/syntax/eval/trace-limit paths against real Pyodide (`runner.test.ts`, 16 green); skulpt-compat appendix note written. Explicitly deferred adjacent item: prefix-adjusting the visible `File "answer.py", line N` numbers (only the `student_line` field subtracts `answer_prefix` today).
- [x] **LD-36 — pink bug icon made real (internal-grading-error dialog).** Internal grader crashes already carry the full traceback as `PedalFeedback.system_error` (`pedal-env.py:169-185` fail-soft → `pedal.ts:49-52`) but reach only `console.error` + the dev-console/footer via `handlers.system` (`engine-adapter.ts:294-298` → `CodingEditor.tsx:543-551`) — no on-demand affordance. The quick-menu bug icon is the legacy-dead element (`QuickMenu.tsx:309-312`; legacy `interface.js:181` had no click/visible binding, only `feedback.js:269` ever `.hide()` it) and sits in the Row-1 right column = literally the top-right corner. Landed exactly as planned: store `graderError` + `setGraderError`; `RunHandlers.systemError` fired from `shapePedalFeedback` (Pedal system_error) AND the grading-job failure branch (PedalEnvironmentError tracebacks), on both the run and on_eval paths; the QuickMenu icon renders conditionally as a faint-pink button (opacity 0.55 → 1 on hover/focus; `display:none` dropped) opening the "Internal Grading Error" Dialog with the traceback `<pre>`; visible to ALL roles; dev-console routing kept; cleared at run start; a cleared error closes a stale dialog. Component test drives set → icon → dialog → clear (20 QuickMenu tests green). Ledger written.

### Milestone 7.2 — Settings actually apply (incl. autocomplete → assignment setting) — **landed 2026-07-14** (slice 2 remains)

Audit result (2026-07-14, full key-by-key table in the investigation record): of the SettingsEditor's rows only **9 are live** (`toolbox`, `docs_url`, `disable_feedback`, `disable_instructor_run`, `disable_tifa`, `preload_all_files`, `can_close`, `hide_evaluate`, `allow_real_requests`) plus `hide_files` partially; ~28 are inert. Eight are inert in LEGACY too (`type`, `part_id`, `disable_trace`, `disable_edit`, `hide_all`, `hide_import_statements`, `hide_coverage_button`, `hide_submission` server-only) — they keep round-tripping and STAY inert per A4. The gap is uniformly in `App.tsx`: SettingsEditor renders/merges correctly, but App only forwards ~10 keys.

- [x] **Autocomplete becomes assignment setting `enable_autocomplete` (default false), REVERSING M3.3/M3.5.** Landed exactly as planned: SettingsEditor row (A4 Studio-extensions table updated, alongside a back-filled `allow_real_requests` row); App prop → `CodingEditor.enableAutocomplete` (effect + on-ready); CM6 Compartment untouched; store flag / `toggleAutocomplete` / localStorage key / toolbar button all removed (orphaned browser keys left unread).
- [x] **The three named dead keys:**
  - `has_clock` — landed: App passes `settingBool(settings['has_clock'] ?? false)` (the hardcoded `hasClock: true` was the bug; QuickMenu's positive-meaning prop unchanged).
  - `hide_queued_inputs` — landed: the one-line App pass to the existing QuickMenu gate.
  - `hide_files` — landed: the whole Row-3 strip (and the tree rail) now gates on legacy `files.visible = instructor || !hideFiles || preloadAllFiles` (`blockpy.js:913-917`), with `preload_all_files` threaded into CodingEditor as its own prop. **`hide_files` defaults TRUE (A4 §5)** — students on default assignments stop seeing the strip, which IS legacy behavior; the always-visible strip was the regression.
- [ ] **Slice 2 — remaining regressed keys, cheapest first** (consumers mostly need wiring, not building; each becomes its own checkbox at implementation): `can_blocks` (App → existing `CodingEditor.enableBlocks`, default true per A4), `hide_trace_button`, `disable_student_run`, `only_uploads`/`only_interactive` (student read-only editor), `hide_editors`, `hide_middle_panel`, `can_image` (Toggle Images gate), `hide_import_datasets_button` (button is a stub — gate it anyway), `start_view` (apply to `pythonMode` at load), `disable_timeout` (engine watchdog off). Deferred WITH their features, not here: `datasets`/`preload_files` (CORGIS, M1.5 pending), `is_parsons`, `save_turtle_output` (turtle), `small_layout`, `instructions_pool` (pools), client `passcode` gate.

### Milestone 7.3 — Engine-boot loading indicator (LD-37) — **landed 2026-07-14** (gated e2e assertion pending)

Root cause: on first Run the button flips instantly to the orange Stop state (`PythonToolbar.tsx:80-90`) and the only "Loading Python engine…" text lands in the footer/dev console (`engine-adapter.ts:106` → `CodingEditor.tsx:543-551`) — a 10–30 s opaque Pyodide download reads as a hang. The adapter ALREADY exposes `onBootStateChange(booting)` (`engine-adapter.ts:49-50`, fired at `:103`/`:149`) — currently dropped on the floor (`App.tsx:626-631` never passes it).

- [x] `onBootStateChange` (now `(booting, label)`) → store `engineBooting: string | null`: the Run button shows the LD-32 spinner + "Loading…" (title = the full message) instead of the inert orange Stop, and the student console renders a status ROW ("Starting Python — one-time setup, may take a little while…") that scrolls with the printer — chrome, not routed system output, so the dev-console rule is untouched; `handlers.system` flows to footer + dev console unchanged. A fatal boot failure clears the indicator (finally-guarded).
- [x] The first-grading Pedal wheel install drives the same indicator with its own message, finally-guarded around `gradeWithPedal`.
- [ ] Gated `PYODIDE_E2E` assertion (indicator appears on first Run, clears when output streams, absent on the second) — rides the next gated e2e run; the store→UI path is unit-covered (Console banner test).

### Milestone 7.4 — Toolbar upload/download + file-button alignment — **landed 2026-07-14**

- [x] **Upload/download made real (LD-39).** Both buttons are disabled stubs (`PythonToolbar.tsx:124-136`; the header comment at `:4-6` lists them as unwired placeholders). Port the legacy flow: Upload = hidden `<input type="file">` (the `Footer.tsx:60-83` FileReader pattern), `accept=".py,.ipynb,.txt,.csv,.json"`; `.ipynb` runs a `convertIpynbToPython` port (`python.js:161-181` — code cells joined, `%`-magic cells dropped, markdown/raw wrapped in `'''`); contents write to the ACTIVE file through the VFS + `handleCodeChange` (autosave/dirty ride along); read-only files refuse (D3-A). Download = Blob + temp `<a download>` (`abstract_editor.js:18-35`); `answer.py` downloads as `sluggify(assignmentName) + '.py'` (`python.js:466-474`), mimetype `text/x-python`. `X-File.Upload`/`X-File.Download` logged at the handler sites (already-legacy event ids). **LD-39 delta:** legacy AUTO-RAN after upload (`python.js:462`) — Studio does NOT (maintainer wording: the buttons "just work locally"); ledgered.
- [x] **Filesystem toggle joins the file-ops cluster.** Landed: the tree toggle moved from far-left (jammed before "View:") to its own `<li>` after `AddNewMenu` at the right end; "View:" stays the A8 first `<li>`; the rail-header collapse button keeps the same icon. Port helpers live in `chrome/file-transfer.ts` (convertIpynbToPython/sluggify/downloadPlan/triggerBrowserDownload) with a 6-test conformance suite; FileTabs/QuickMenu suites re-verified.

### Milestone 7.5 — Block editor: semicolon statement groups — **landed 2026-07-14**

Root cause: `@lezer/python` parses `a = 1; b = 2` as a legal `StatementGroup` node (grammar `python.grammar:83-86`) — no error node, so the B3 gate passes — but `to-ast.ts statement()` has no case for it and throws `unsupported statement StatementGroup` (`to-ast.ts:260-261`), which the chop-and-retry loop turns into a raw block from that line to EOF (nested bodies break identically via `body()`, `to-ast.ts:348-354`). Legacy BlockMirror DID support semicolons (the Skulpt AST yields two same-line `Assign`s; its grindlehook demo is full of them) and also normalized `;` → newlines on the return trip — so support is parity restoration, not a delta.

- [x] `StatementGroup` case added in `statement()`: children flattened (skipping `;` tokens), sub-statements carrying the shared line — the Skulpt shape; nested bodies covered via the same path. `measureNode`/`convertBody` needed no changes.
- [x] Round-trip: `;` → newline normalization asserted by a dedicated 7-test NON-exact-text suite (`semicolons.test.ts`) — top-level, trailing `;`, nested bodies, no-rawify-after regression; corpus untouched (146 blocks tests green). Found en route, both PRE-EXISTING legacy generator behaviors (not semicolon artifacts, documented in the suite): `pass` statements drop (BlockMirror has no Pass block; `pass` re-synthesizes only for empty bodies) and `ast_ReturnFull` always parenthesizes its value.

### Milestone 7.6 — Chrome & quiz UX polish — **landed 2026-07-14**

- [x] **Feedback rating pinned bottom-right in both states (LD-38).** The expanded rating is in-flow (`Feedback.tsx:159-160`, `textAlign` only) while collapsed is absolute-pinned (`:181-184`) — and `.blockpy-feedback` isn't `position:relative` (`blockpy.css:319-324`), so even the collapsed pin anchors to a distant ancestor. Fix structurally rather than absolute-positioning both: `.blockpy-feedback` becomes a flex column with an inner scroll wrapper (message + positives, `overflow-y:auto`, flex-1) and the rating as a pinned footer row aligned right — visible while long feedback scrolls, no content overlap, and the "Thank you!" animation (`blockpy.css:654-660`) keeps floating above it. Legacy used the identical split-personality layout, so this diverges from parity → ledgered with the usability rationale (B6).
- [x] **Draggable dialogs (parity restoration, no LD).** Landed: pointer-events drag on `.modal-header` (pointer capture, `translate` on `.modal-dialog`, `touch-action:none`, `cursor:move`, the × excluded from the handle), offset re-centers on each open. One shared component fixes all six call sites (fork dialog makes seven); theme-skin classes intact.
- [x] **Focus mode: console/feedback drawer open by default (amends LD-24).** Landed: `drawerOpen` defaults true and `setFocused` opens on entry; non-persisted per the LD-24 posture. Drawer test updated (open → collapse → badge re-expand).
- [x] **Quiz font-size control (LD-40).** Landed: `--quizzer-font-size` on the surface root consumed by `.quizzer-question-card` (inline blanks/selects inherit); A−/A+ stepper (1/1.15/1.3/1.5 rem) at the top-right of the quiz; persisted `BLOCKPY_display.quizFontSize` with storage guards.

### Milestone 7.7 — Dark theme covers the group navigation (amends LD-23) — **landed 2026-07-14**

Root cause: `data-theme` is already GLOBAL (`document.documentElement`, `store.ts:269-275`) — scoping is not the problem; the dark scope (`themes.css:20-146`) simply has no rules for the nav, whose colors are hardcoded: `.assignment-selector-btn:not(.btn-success) { background:white; color:black }` + disabled `#e0e0e0` (`navigation.css:59-67`), the warning-yellow strip (`:27-32`), the `.correct/incorrect/secret-submission` option colors (`:42-52`), and bootstrap-subset `.btn`/`.btn-outline-secondary` text colors.

- [x] Explicit `:root[data-theme='dark']` overrides landed IN `navigation.css` (component-owned theming; data-theme is already global): strip surface/border (dark parchment), selector buttons + disabled state, the assignment `<select>`, and the three submission-status colors on the themes.css dark ramp. Win2000 stays editor-scoped by design.
- [ ] Extend the M6.1 axe contrast gate to the dark-mode nav (rides the next e2e pass).

### Milestone 7.8 — Textbook chapter navigation — **landed 2026-07-14**

Investigation verdict: chapter HEADER rows are non-clickable BY DESIGN in legacy AND Studio (`textbook.html:60-75` binds click only `if item.reading`; `Textbook.tsx:224-250` replicates, incl. header+reading rows staying clickable) — the report is either about that design or about readings collapsing to disabled "Missing Reading" rows when the `loadAssignmentByUrl` endpoint is unpublished (the LD-16 resolver returns null → `missing:true` → no handler; CSS is not implicated). Fix both:

- [x] **Chapter headers become useful (LD-41, net-new):** rows with children get a chevron and toggle their subtree; header-only rows toggle on row click; header+reading rows keep opening their reading (chevron = the separate collapse affordance); `aria-expanded` reported; default expanded, per-session state. Component test drives collapse/expand + nested independence (16 textbook tests green).
- [x] **Missing-reading diagnosability:** instructor views annotate Missing Reading rows with the unresolved url + the LD-16 hint (tooltip + inline "(unresolved — LD-16?)") instead of a silent disabled row; the server-team ask (publish `loadAssignmentByUrl`) stays flagged under M6.4.

### Milestone 7.9 — Fork flow for non-owned assignments (LD-42) — **landed 2026-07-14** (client complete; server-team flags in the ledger)

Investigation: Studio has NO fork implementation — only the reserved `forkAssignment` URL key (`context.ts:21`); ownership fields (`owner_id`/`course_id`/`forked_id`/`forked_version`) survive only in `assignment.raw`, undecoded; a non-owned instructor save currently **fails silently** in submission-sync. Legacy's flow: saveFile failure carrying `response.forkable` → `startPossibleFork` (`server.js:657-661`) → `OFFER_FORK` dialog (`dialog.js:161-190`) — but legacy's dialog buttons had NO click handlers (dead UI, like the bug icon). The server route works and is tested (`POST /assignments/fork`, `assignments.py:133-177`, instructor-guarded, group-move + url + new-owning-course options).

- [x] **Server contract verified (2026-07-14, source-level — stronger than the planned fixture):** `forkable: true` is emitted ONLY by `save_assignment` via `require_course_instructor(…, allow_fork=course_id)` (helpers.py:55-60, blockpy.py:942) — instructor in their own course, assignment owned elsewhere. Instructor-file `save_file` fails WITHOUT the flag (blockpy.py:275-288 → covered by the proactive notice). CRITICAL find: the templates' published `forkAssignment` URL points at `blockpy.fork_assignment` (editor.html:205), a half-finished route that never returns the fork's id (blockpy.py:1139-1178); the WORKING route is `/assignments/fork` (assignments.py:133-178, forks into the CALLER's course). Also: `assignment_group/fork` requires instructorship in the OWNING course — unusable for the non-owner scenario, so "Fork entire group" ships only when the server grows a route (flags in LD-42).
- [x] **API:** `ApiClient.forkAssignment(assignmentId, {group?, url?})` on the reserved key (working-route wire contract; owning course from the base payload), `isEndpointConnected`-gated + read-only-guarded; callers treat a response without a numeric `id` as failure (survives the broken deployed route gracefully). `DecodedAssignment` gained `ownerId`/`courseId`/`forkedId`/`forkedVersion`.
- [x] **Client flow (legacy's dead buttons made real — LD-42):** the App's `save_assignment` path inspects responses for `forkable` → OFFER_FORK dialog ("Assignment Not Owned; Fork?"; fork-this-assignment with optional new URL — collisions surface the server message; transfer-submissions/group-fork deferred to the server flags) → on success the app navigates to the fork through the host dispatch. Proactive layer: instructor view of a non-owned assignment (decoded courseId ≠ context course) shows the notice + fork affordance BEFORE the first rejected save; when the endpoint key is unpublished it explains that instead.
- [x] Tests: `fork.test.ts` (payload shape incl. base-context course, capability/read-only gating, ownership decode incl. absent→null) — 36 api tests green. _Dev-stub `forkable` smoke deferred with the stub's instructor modes._

**Exit criteria:** all sixteen review items closed or explicitly ledgered (LD-35…LD-42 written); suites green including the updated grading/round-trip/drawer tests; the LD-35 and by_url server-team flags filed; gated boot-indicator and fork smokes passing.

**Status (2026-07-14): all ten milestones landed same-day.** LD-35…LD-42 written; A4 gained `enable_autocomplete` (+ back-filled `allow_real_requests`); skulpt-compat documents the `<exec>` strip. Residuals, each tracked on its milestone: the gated `PYODIDE_E2E` boot-indicator assertion, the dark-nav axe extension (both ride the next e2e pass), M7.2 slice-2 keys, and the LD-42 server-team flags (repoint `forkAssignment` at `assignments.fork`; group-fork route; LD-35 server mirror).

**M7.10 — Engine crash recovery (landed 2026-07-15, LD-43).** Field report: second `input()` run fataled ("Maximum call stack size exceeded") after a Pedal 3.0.1 `cait_node.__getattr__` runaway (`prevent_operation`; Pedal fix upstream by maintainer). Root causes fixed in Studio: interpreter reloads dropped the init `indexURL` (the "`<!doctype` is not valid JSON" permanently-dead engine); grading fail-softs swallowed fatals so the corpse met the next run; no defense against poisoned-but-alive interpreters (pyodide#5987). Now: reloads reuse the indexURL, a post-job stack canary replaces dead/poisoned interpreters between jobs, recovered fatals present as `EngineCrash` with a student-readable message, `runner-reloaded` re-arms the Pedal install path (client respawns too), and WorkerHost serializes message handling so queued jobs wait out a reload. Gated e2e: grader-fatal-then-healed-run (§6.6 canary).

---

## 9. Cross-cutting workstreams (run through all phases)

| Workstream                          | Cadence                                                                                |
| ----------------------------------- | -------------------------------------------------------------------------------------- |
| Golden-transcript replay in CI      | From Milestone 1.2 onward, on every PR touching `api`/lifecycle code                   |
| Approved-differences ledger (§16.2) | Reviewed at each milestone exit                                                        |
| Skulpt-compat appendix (§6.7)       | Updated whenever an engine delta is found                                              |
| Performance budget tracking (§16.3) | Dashboard from Milestone 1.3; regression alerts                                        |
| Accessibility review                | Component-level checks at each UI milestone; full audit in Phase 6                     |
| Lint & standards gate (Phase 5)     | Hard zero-warning CI gate from M5.1; CODE_STANDARDS.md reviewed at each milestone exit |

---

## 10. Risk register

| #   | Risk                                                                                                                   | Impact                                            | Mitigation                                                                                                                   |
| --- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| R1  | ~~Pyodide unusable in LMS iframes without COOP/COEP~~ RESOLVED: SAB confirmed dead in Canvas; compat mode is primary   | Interrupt latency / async-input UX in compat mode | Compat-path-first engine design (M1.3); worker-termination hard stop as floor; SAB as enhancement                            |
| R2  | Pedal/TIFA behavior differs on CPython vs Skulpt, breaking existing graders                                            | Curriculum regressions at scale                   | Engine regression corpus (§16.1.3) early; shim in the Pedal integration layer, never the engine core (§6.7)                  |
| R3  | Prefix/visibility semantics mis-transcribed from `files.js`                                                            | Silent data corruption on the wire                | A1 is a blocking Phase-0 gate; adapter is a single module with a conformance fixture suite                                   |
| R4  | Lezer CST diverges from CPython grammar (error tolerance, edge constructs), so blocks disagree with what actually runs | Blocks mode unusable or misleading for courses    | Spike S2 corpus validation; round-trip suite (§16.1.2); tests cross-check Lezer parse success against CPython `ast` verdicts |
| R5  | Event vocabulary drift breaks research pipelines                                                                       | Loss of research data continuity                  | A2 review gate; only `X-` extensions allowed (§14.4)                                                                         |
| R6  | Quiz schema richer than sampled fixtures                                                                               | Data loss on quiz save                            | Round-trip-unknown-fields rule (§11.3.7, §14.5); widen A3 sampling across production courses                                 |
| R7  | Pyodide first-load latency hurts readings/quizzes                                                                      | Bad first impression on non-coding assignments    | Readings/quizzes interactive before engine loads (§16.3); lazy engine boot                                                   |
| R8  | Textbook port slips                                                                                                    | Phase 2 delay                                     | Pre-approved fallback: legacy-shim island (§11.4)                                                                            |
| R9  | Real-network `requests` (M3.5) limited by browser CORS                                                                 | Setting appears broken for arbitrary URLs         | Default stays the mock; document best-effort; consider a server proxy / allowlisted hosts later                              |
| R10 | Group organizer slice 2 blocked on missing server endpoints (type change, subordinate JSON, live position ordering)    | M4.6 slips                                        | Slice 1 ships on existing endpoints; server-team flags filed LD-16-style; capability-detected controls                       |
| R11 | Themes (M4.1) drift from the B6 visual-parity mandate                                                                  | Parity regressions in the default look            | Light stays default + normative; themes are explicit opt-in; overrides live in tokens only                                   |
| R12 | `match` block design has no BlockMirror precedent                                                                      | Round-trip regressions in blocks mode             | v1 textual patterns; corpus fixtures gate merges exactly like §16.1.2                                                        |
| R13 | LD-35 quiz-grading fix diverges client vs server until the server mirrors it (real submissions still hit the skip hack) | Try-It/demo grades disagree with recorded grades  | Client fix affects only local grading (editor Try-It, static demo); server-team flag filed with the fix diff; ledger documents the interim divergence |
| R14 | Fork flow (M7.9) depends on the server emitting `forkable` on rejected saves — unverified on the deployed server        | M7.9 slips or mis-triggers                        | Blocking contract-verification step first (A5 fixture extension); fallback gate = decoded ownership comparison               |

---

## 11. Suggested build order (dependency-driven)

```
Phase 0:  scaffold ──► A1..A7 appendices ──► spikes S1..S3
Phase 1:  vfs ──► api ──┐
          engine ───────┼──► editor+blocks ──► integrations ──► mountLegacy (flagged ship)
Phase 2:  AssignmentHost ──► navigation ──► reader ──► quizzer ──► lti-embed/shim/islands (flagged ship)
Phase 3:  M3.0 py-files ──► M3.1 blockly-unbreak ──► M3.2 grading/console ──► M3.3 chrome ──► M3.4 quiz-css
          M3.5 settings-editor │ M3.6 match-blocks │ M3.7 files-ux   (parallel once M3.2 lands)
Phase 4:  M4.1 themes ──► M4.2 focused-mode ──► M4.3 docs-panel
          M4.4 csv/json ──► M4.5 image-editor │ M4.6 organizer │ M4.7 textbook-routes   (independent tracks)
Phase 5:  M5.1 lint-zero (starts immediately, any time) ──► M5.2 blocks-typing ──► M5.3 standards-doc
Phase 6:  hardening ──► default-on
Phase 7:  M7.0 quiz-grading ──► M7.1 tracebacks ──► M7.2 settings ──► M7.3 boot-indicator
          M7.4 toolbar-files │ M7.5 semicolons │ M7.6 ui-polish │ M7.7 dark-nav │ M7.8 textbook   (parallel once M7.2 lands)
          M7.9 fork  (server-contract verification first; otherwise independent)
```

`vfs`, `api`, and `engine` milestones can proceed in parallel once Phase 0 lands; `editor` needs all three. Within Phase 2, `reader` depends on the minified editor (built in 1.4) and `quizzer` depends on the engine's `quiz.preprocess` phase (built in 1.3). Within Phases 3–4: M3.0 precedes anything touching the runtime Python (M3.2, M3.5); M3.7's grid generalization precedes M4.3's right rail; M4.1 precedes M4.2 (focused mode must respect themes); Phase 5 runs whenever, but M5.2 should trail M3.6 so the blocks typing pass covers the new match/async converters once instead of twice. Within Phase 7: M7.2's `hide_files` strip gate precedes M7.4's tab-strip relocation (same FileTabs region); M7.7 rides the M4.1 token system; M7.0 and M7.6's quiz font control both touch the quizzer but are independent; M7.9's client work waits only on its own server-contract check. Phase 7 precedes the M6.4 default-on gate — the punch list is part of what "default-on ready" means.

---

## 12. Definition of done (project-level)

1. All five conformance suites (§16.1) green in CI.
2. Golden-transcript replay matches modulo the approved-differences ledger (§16.2 / G3).
3. Non-functional acceptance criteria met (§16.3).
4. Frozen appendices published: prefix table, event vocabulary, quiz schema, settings inventory, Skulpt-compat deltas.
5. At least one full semester course runs default-on with the legacy client available as fallback.
